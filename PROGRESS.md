# PA_Agent 工单进度

- WO-F Claim Validation 已由提交 `c0b58d0` 推送到 PUBLIC `origin/main`；本地、远端跟踪分支和 GitHub SHA 一致，工作区在发布后干净。
- 完成证据复审补齐了四类直接回归：system prompt UTF-8 字节不变；固定代理回滚时 10981 被外部进程抢占会失败关闭；长桥重复分页、夏令时、半日市和大周期跳过；Campaign 记录丢失、非法状态，以及撤单/离场/最终刷新/中断异常耐久收口与零重复写。
- 最新三套件为 1956 项通过、7 项跳过、0 失败；四个改动测试文件合并定向回归为 220 项通过、0 失败。
- 全仓 Ruff 只读基线已刷新为 293 项诊断，其中 249 项带自动修复建议；这是历史债账本，不授权全仓 `--fix`。
- WO-E 设计合同已补齐 `QuoteSnapshot`、新鲜度、四市场设置迁移、generation、Longbridge 内切换、Longbridge↔OKX 跨源回滚、脱敏输入和 17 个验收场景；没有代选方向、调用后续设计工具或修改 PyQt6。
- WO-A 仍有一项未闭合：固定代理 metadata 与实际配置缺共同指纹。只改测试无法证明不存在不一致，本轮又禁止修改 `scripts`。
- WO-D 的最后已知外部结果仍是长桥 `401004 token invalid`；共享 `env` 自 2026-07-24 后未更新，没有可重跑真实三标的验收的新凭据证据。
- WO-E 生产实现仍等待用户在 A/B/C 中选择，当前推荐 B“证据优先”；最终三标的桌面验收还依赖有效长桥凭据和用户从快捷方式启动。
- WO-H、WO-C2、WO-F、WO-G-1/2/3 已完成；成交量仍不进入提示词或交易判断。
- `WO-POS-05` 只有路线图级目标且会触碰当前禁止修改的执行链，本轮不越界开工。
