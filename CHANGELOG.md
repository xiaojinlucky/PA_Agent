# 更新记录

## 0.1.0（发布候选，尚未发布）

这是 Windows + Python 3.12 的源码部署版，不是 EXE、安装器或 PyPI wheel。

### 新增

- 原生 PyQt6 四市场只读看盘页：加密、美股、港股、A股。
- 10m 主分析周期，以及可缺失的 1h/4h 背景。
- Longbridge/OKX 独立只读行情运行层与统一分析截止时间。
- 五层能力证据索引、脱敏证据格式、完整构建 SHA 和离线自检。
- Windows 源码安装、卸载、快捷方式、Release 校验和失败关闭发布脚本。
- 提交完成后生成的外部稳定证据包，避免证据文件引用自身 Git SHA 的不可能循环。

### 安全

- Worker schema v5 将一次新增风险授权耐久绑定到唯一命令。
- 顶层窗口在真实关闭事件前保持有效；图表先停止定时刷新和绘制，再由 Qt 父对象销毁，避免退出时访问已删除坐标轴。
- 三份 Windows 发布脚本使用 UTF-8 BOM，保证 Windows PowerShell 5.1 不会把中文安全提示读坏。
- Windows PATH 同时存在多个 Git 时，源码安装脚本明确选择首个可执行程序，不再把多个路径拼成无效命令。
- 候选发布的离屏证据文件名与验证合同统一使用 `1p0/1p25/1p5` 三档缩放标识。
- 发布证据扫描器区分 HTTPS 地址与 Windows 盘符路径；完整测试报告不再把密钥反例样本写进用例名称，同时保留真实路径和密钥的失败关闭检查。
- CI 与候选发布在上传证据前脱敏 JUnit 中的 Runner 绝对路径，并保持测试数量、失败数、错误数和跳过数不变；脱敏扫描或文件完整性检查失败时，CI 不上传原始证据。
- 源码 ZIP 只排除仓库根的日志、记录、经验和交易记录运行态目录，同时完整保留 `pa_agent/records` 生产代码；运行态 `.gitkeep` 也不能绕过扫描。
- 股票没有权威最小变动单位时只展示，禁止价格分析。
- 成交量继续只做影子研究，不进入提示词、信号、仓位或风险门。
- 交易默认关闭；v0.1.0 明确不发布 OKX Live 或 Longbridge 交易能力。
- 新装和工作台保存都固定为 OKX Demo 路由；Controller 与 Worker 双重拒绝其他新增风险路线。

### 尚未通过的稳定发布门

- 运行 Worker 尚未在新鲜 OKX Demo 私有只读硬门后加载 schema v5。
- 正式快捷方式的 Longbridge 三市场、Crypto、快速切换和缩放矩阵尚未由用户验收。
