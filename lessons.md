# Lessons

- 2026-07-14：用户要求安装、配置现有开源项目时，先保持上游源码原样；未经明确要求，不扩展实盘能力或主动改造安全实现。必须区分“运行时配置”和“修改项目代码”，披露原项目限制后停在必要决策点。
- 2026-07-15：用户说“参考其他项目的前端优化”时，指复用“网页版 GPT 独立审查 → Google Stitch MCP 生成/迭代设计 → 人工验收 → 再落地代码”的流程，不是要求 PA_Agent 复制参考项目的视觉风格。
- 2026-07-16：PA_Agent 的首轮 Stitch 方案视觉完成度不足。后续前端重设计应先拆解 `D:\Desktop\Quant\前端设计\长桥` 的真实页面素材，提炼长桥式的留白、层级、卡片/表格密度、导航与行情配色，再把明确视觉规则交给 GPT/Stitch；不能只写“参考长桥”或在现有 QSS 上换颜色。
- 2026-07-16：对已有产品做 Stitch 重设计时，必须先截取当前产品全部关键界面，并从代码整理“页面—功能—状态—交互—不可丢失契约”；把这些现状材料作为产品真源提交给 Stitch，外部产品截图只能作为视觉语言参考。不得只给外部参考图就让 Stitch 猜测现有产品功能。
- 2026-07-16：用户说“目前在 OKX 里只做 XAUUSD”时，是要求优先完成黄金的现货类产品与永续合约交易链路，同时保留后续扩展其他品种的架构；不能曲解为永久硬拒绝所有非黄金品种。
- 2026-07-16：PA_Agent 与 AlphaMaster 虽然都用于量化投资，但属于两个完全独立的项目。上下文、Git 提交、验证、文件包、模型任务和未来架构必须分别处理；未经用户另行明确授权，不得设计共享执行层、跨项目信号接入或统一交付任务。
- 2026-07-16：网页版 GPT 通过 GitHub 给出的方案看不到本机 Codex 的 skills、memory、未提交状态、进程和私有配置，因此只能作为外部规划输入。正式执行前必须由本地 Codex 按当前工作区和 `docs/LOCAL_EXECUTION_CONTEXT.md` 校正模块路径、工具能力、命令、验证方式与安全边界，不能直接照搬。
- 2026-07-16：PA_Agent 实盘目标不是增加一次性“下单 API”，而是把 PA 分析结果中的入场、止盈、止损等指令接入 Longbridge 与 OKX，覆盖订单受理/成交、持仓监控、止盈止损或主动离场、账户资金与已实现/未实现盈亏读取，并把状态回写 PA。验收必须证明这条完整生命周期；同时只实现该闭环所需的最小模块，不扩张为通用交易平台。
- 2026-07-16：第三方平台开发者已明确确认某项集成当前不可用时，应停止重复授权和连通性测试，把该限制记录为外部边界；不得继续把问题归因于用户权限配置。
- 2026-07-17：大模型供应商首轮接入必须收敛到用户明确点名的 Codex 订阅、Kimi API、DeepSeek API 和小米 MiMo API；PA_Agent 复用现有模型档案，不为“以后可能支持”重写通用框架。验收要区分“请求已正确到达供应商”和“账户有额度并生成回复”，不能把供应商余额不足误判为代码故障。
- 2026-07-17：大模型适配器存在于后端、下拉框里能选，不等于前后端已经对用户可用。必须把用户要用的档案实际保存到配置、逐个测试并让前端列表可见；否则应明确说“后端已接入、前端尚未完成配置”，不能笼统宣称已经统一。
- 2026-07-17：订阅制档案显示“已测试”仍不等于订阅链路可用。必须实际覆盖“无 API Key 激活档案 → 主界面不再弹 API Key 门禁 → 保存并测试 → 发起一次真实分析”完整路径；订阅登录状态、网页登录/设备码登录入口和 API Key 配置必须在界面与后端语义上彻底分开。
- 2026-07-17：排查“极度激进没有下单”时，必须同时核对模型原始阶段二输出、归一化后的最终决策和执行账本。模型已经给出交易方案却被程序规则改成不下单时，必须报告为“程序拦截”，不能误报为“模型没有产生下单决策”。
- 2026-07-17：PA 前端改版必须先把当前主界面、模型设置和实盘生命周期作为功能真源交给 Gemini Stitch；OmicOS 只参考供应商分组和信息层级，不能照抄品牌风格。Codex 不直接凭审美改界面，必须先验收 Stitch 设计没有删除交易能力，再落 PyQt6。
- 2026-07-17：模型上下文上限是模型能力元数据，不是普通档案的自由输入项；累计多次请求的 Token 用量也不是当前请求的上下文占用。界面应只读展示可靠上限或“尚未确认”，并把累计用量与单次请求上下文分开。
- 2026-07-18：模型自动拉取成功后，账号接口返回值才是可选真值；内置目录只用于首次加载、失败兜底和为相同模型补能力元数据，不能把账号未返回的旧模型混入下拉框。刷新失败必须保留原列表，模型能力变化必须使旧测试失效。
- 2026-07-18：API Key 的“显示”状态不能跨供应商或档案继承；切换后必须立即重新遮罩。模型目录后台结果还必须绑定适配器、地址和 API Key 指纹，防止旧账号的迟到结果覆盖新账号界面。
- 2026-07-18：自动测试必须用正式 `config/settings.json` 的前后 SHA-256 作为硬隔离门禁，并把默认保存路径延迟到运行时解析；测试曾短暂写到正式配置时，即使随后恢复，也不能把该轮测试视为安全通过。
- 2026-07-19：Codex 能拉取真实模型列表，只证明“模型目录”可用，不证明订阅登录、真实回复、档案保存、运行时激活和重启恢复已经贯通。以后只有在真实 GUI 链路完成“检测登录 → 测试并保存 → 激活 → 重启读取”且全程不要求 API Key、不关闭重开设置窗口后，才能宣布 Codex 订阅接入完成。
- 2026-07-20：Codex 断线重连后，排队消息可能越过尚未完成的前置任务。恢复时不能按聊天顺序假定已经完成，必须重新建立“代码存在 / 自动测试 / 真实外部调用 / 用户可见界面 / 生产配置”五层完成矩阵；任何一层缺失都要明确标为部分完成或未完成。
- 2026-07-20：交易系统设计不能先假定必须自己实现。先检索小红书、微信、抖音/B站等真实用户社区中的故障，再按“成熟开源社区工具 > 小范围高质量工具 > 必要的项目内适配层”核验许可证、代码、测试和真实边界。商业闭源产品只借鉴工程分层，开源项目只有在代码语义与 PA 的账户、订单、恢复和风险边界一致时才复用；明星数或宣传文章不能替代代码级审查。
- 2026-07-20：`needs_attention` 只是异常现象，不等于写权限类别。新增风险租约、减险写入和持久停写必须分别建模；券商明确拒绝后还要结束旧运行意图，否则重启会把“确定没写入”误判成“结果未知”，造成永久停写。验收必须覆盖“拒绝 → 重启 → 无写只读对账 → 清标 → 下一次人工操作”完整链。
- 2026-07-20：用户修改 GitHub 用户名后，不能依赖旧远端重定向或旧“私有仓库”记忆。任何提交/推送前都要实时核对登录账号、新仓库地址、可见性、权限和远端 SHA；当前 `xiaojinlucky/PA_Agent` 若仍为公开仓库，就必须停止推送。
- 2026-07-20：大模型档案“真实测试通过”后激活失败，不应只排查登录或模型 ID，还必须核对共享 `settings.json` 的 revision。Windows 文件锁只能可靠约束外部进程，PA 同一进程内的并发线程还需要独立线程锁。行情/周期等非 AI 设置在测试期间保存时，应在同一临界区保留磁盘最新普通设置并只合并本次已验证 AI 档案；只有另一窗口也修改 AI 档案时才拒绝覆盖。验收必须覆盖“Sol 活动 → Terra 测试通过 → 普通设置并发保存 → 激活 → 重载仍为 Terra”以及“合并读取后第二线程不能插入写入”。
- 2026-07-20：Windows 提权脚本不能只用当前 PowerShell 7 验证。实际 UAC 会调用 Windows PowerShell 5.1；隐藏、非交互宿主可能不支持 `Start-Transcript`，5.1 也不能解析依赖 PowerShell 7 自动续行的换行管道。提权前必须先用目标 `powershell.exe` 做只解析检查，日志写普通结果文件，成功后再复扫实际 ACL 和服务进程树，不能凭管理员进程启动就宣称完成。
- 2026-07-20：不能在 `PA_AGENT_PAPER_TRADING_ENABLED=false` 或服务尚未重新加载该值时，把 Longbridge/OKX 模拟链路描述成“界面可直接启用”。交付前必须同时核对共享 env、WinSW 进程环境、Worker 最近成功对账和全部活动 execution；若历史 Demo 仓位未安全收口，应先只读确认订单/保护真值，再显式减仓离场，不能为消除弹窗直接放开新风险。
- 2026-07-23：5 分钟自动循环正在运行，不等于每根 K 线都会产生订单。向用户报告时必须把“循环活着、AI 已分析、AI 决策不下单、计划被程序拦截、订单已提交、订单成交或撤销”分开写；用户在券商账户看不到后续订单时，先核对每根 K 线的原始阶段二决策、归一化结果、计划构建和券商账本，不能只说“后台还在跑”。
- 2026-07-23：为快速验收订单链路而把一个远离现价的限价计划直接改成市价单，会让原止损/止盈相对实际成交价失真。正确做法是只在运行器专用提示中要求模型按最新 K1 附近重新构造完整三价；若 §14 仍判定无合法即时交易，应报告为策略不下单，而不是暗中放宽硬性禁止项制造成交。
- 2026-07-23：当用户要尽快看到完整订单生命周期、但真实策略连续判定“不下单”时，必须把“策略交易”和“Demo 技术闭环验收”拆开。验收单要明确标记、固定 Demo 路由和数量、只经现有 Controller/Worker，且不能被后续策略循环误读为真实分析；这样既能快速验证入场、保护和离场，也不污染策略效果判断。
- 2026-07-23：用户明确要求多周期必须结合真实 PA 原始资料推进。首轮按“高周期背景标签实验 → 极薄 Session 标签 → 成交量维持现状”推进；`15m` 主周期先接 `1h` 和 `4h`，高周期只解释嵌套趋势/区间/位置，不能机械否决主周期，也不能把所有周期整表塞进模型。正式实现前先核对用户提供的原始资料和识别文本，再把事实映射到提示词字段、代码路径和硬验收。
- 2026-07-23：用户要求“限价单加滑点”必须随当前波动变化，不能固定基点。当前执行语义是分析节点捕获的主周期 `ATR14` 乘以可配置倍数；缺 ATR 就直接阻断，入场与主动离场仍分别选择 `limit`、`limit_with_slippage`、`market`，原生止盈止损保护与主动离场方式分开验收。
