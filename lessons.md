# Lessons

- 2026-07-29：用户要求网页版 GPT 基于仓库做“全量审查＋给出推进方向”时，外部审查与执行工单必须拆成两个独立对话。第一阶段只判断仓库信息是否足够、逐项核验项目现状、给出有证据和先后顺序的推进方向；不得同时要求补丁、`/goal` 工单或运行态操作。第二阶段再由全新对话接收完整审查结果，并结合本机实时进程、数据库、凭据和 Git 状态转成执行工单。不能为了显得推进快，把审查者、补丁作者和运行态执行者强行塞进同一提示词。

- 2026-07-29：Longbridge 默认 `LONGBRIDGE_*` 凭据被服务端拒绝，不等于 Longbridge 整体或大 A 行情不可用。先只读检查 `Quant\env` 中同一用途的完整命名档案，再用官方 `QuoteContext` 在原位置做不输出凭据的最小沪深报价与 K 线验证；只把服务端真实接受的档案判为可用。行情源若需改用命名档案，必须通过非机密的显式选择项完成，禁止静默猜测、跨组拼接或把真实 token 写入代码、测试、日志和 Git。

- 2026-07-28：外部设计工具“成功生成”不等于视觉门通过。用户已明确否决 WO-E 的 Stitch 结果，后续不得继续围绕被否决稿精修，也不得把项目 URL、附件成功或生成回执写成审美通过。应改用网页版 ChatGPT Images 2.0 生成三张独立的 1440×900 样板，等待用户选择；选中图必须先拆成原图、标注图、机器可读设计令牌、组件状态、资产清单和前后端映射，再按同尺寸真实渲染做视觉差异审查。

- 2026-07-28：用户是双屏工作流，左屏 Codex、右屏本机 Chrome。用户要求“不占用鼠标”时，不得把“激活 Chrome”误写成会妨碍左屏操作，也不得要求用户反复置前；应精确绑定右屏目标窗口，只使用不移动物理鼠标的页面元素、无障碍或键盘接口。用户截图已经证明右屏 Stitch 地址和五附件显示时，以截图为当前事实，不再重复质疑。

- 2026-07-28：浏览器权限、登录态和页面连通性都属于用户侧可变状态，不能把“用户还没在对话里回复已开启”推断成“权限仍未开启”，也不能把一次新标签页 `ERR_CONNECTION_CLOSED` 扩大成“内置浏览器不可用”，更不能据此重复报告阻塞或把 Goal 标记为 blocked。用户明确陈述或截图证明最新状态后，以该证据为准，优先接管已经打开且可用的目标标签页，不再用新建页面的瞬时失败否定现有页面。

- 2026-07-26：用户不接受任何副标题或注释性小字。PA_Agent 前端不得出现 eyebrow、tagline、标题下解释、灰色辅助小字、括号注释、营销口号、重复说明或小号页脚；界面层级改用主标题、分组、留白、对比度、图标和组件位置表达。完成操作或安全判断必需的字段标签、单位、时间、状态、错误和风险阻断原因可以保留，但必须使用正常可读字号和清晰对比度，不能降级成小字说明。

- 2026-07-25：用户明确要求 Google Stitch 使用本机 Chrome 时，必须通过已登录的 Chrome 会话打开和提交设计，不得继续使用应用内浏览器。Stitch 只负责视觉探索；生成稿若与本地信息架构或真实运行语义冲突（例如重复左导航、错误热生效或写死止盈档数），本地 Codex 必须以现有 PyQt6、执行链和耐久配置语义为准修正后再落地。

- 2026-07-25：新增定仓模式不能只改 Campaign 的数量计算。不可变模式、固定张数和完整风险结果必须同时进入分析快照、ExecutionPlan、配置指纹、Worker 写前复算和杠杆授权；写前不仅比较最终张数，还要比较实际风险金额、合约名义价值和单张最坏损失。否则单张风险变化但取整后张数没变时，旧授权仍可能错误放行。

- 2026-07-25：自动恢复“每根 K 线最多一次”必须是崩溃后仍成立的耐久事实，不能只靠进程内计数。Campaign 要先落本根恢复标记、再入队命令、再落命令 ID；超时后只等待同一 ID。若崩溃在入队与 ID 落盘之间，宁可跳过本根，也不能猜测未入队后创建第二条恢复命令。

- 2026-07-25：用户明确禁止 Agent 操作桌面和鼠标后，GUI 开发与验收只能使用离屏渲染和用户截图；正式桌面窗口由用户自己从 `D:\Desktop\PA_Agent.lnk` 重启。后台 Worker/Campaign 进程控制不等于桌面自动化，但每次仍须先核对精确进程、空仓、空挂单、活动执行和未解决写命令。

- 2026-07-25：常驻 10 分钟 Campaign 不能依赖人工反复解除普通网络抖动。只有严格白名单内的临时私有读取故障，才允许每根新 K 线排一次专用恢复命令；该命令必须由唯一 Worker 先完成新的账户、身份和账单链读取，并保留原高水位。恢复失败就跳过该根 K 线；回撤、身份变化和账本完整性故障永不自动解除。

- 2026-07-25：提交命令 `FAILED` 且 execution 已进入 `BLOCKED`，表示确定没有形成券商订单，不能让 Campaign 误报为结果不明并退出。Campaign 应把该 K 线耐久记为风险阻断后继续下一根；只有 `UNCERTAIN` 或执行记录未进入安全终态，才进入人工处理。

- 2026-07-25：Campaign 安全重启不能只继承旧状态里的 `last_completed_bar_ms`。如果崩溃发生在已创建 execution 之后、状态仍保留 `inflight_bar_ms`，必须从该 execution 的耐久分析记录核对所属 K 线；execution 已是安全终态时应把该 K 线一并继承，避免重启后重复分析或重复交易。

- 2026-07-24：磁盘代码、WinSW Worker、Campaign 和已经打开的桌面 GUI 可以处于不同加载时间；旧 GUI 不会热更新，因此会造成“像是两套系统、界面修改不生效”的错觉。桌面入口统一以 `D:\Desktop\PA_Agent.lnk → 本仓库 .venv\Scripts\pa-agent.exe` 为准，界面必须显示仓库路径和本次代码加载时间；每次 GUI 改动验收都要在安全前提下重新从该入口启动，并让用户通过界面状态自行核对。

- 2026-07-24：`pytest-qt` 默认会创建真实 Windows 窗口，测试数据里的 `test` 会被用户误认为生产 PA Agent 弹窗。所有 Qt 测试必须在测试模块收集前设置 `QT_QPA_PLATFORM=offscreen`；同一导入前阶段就要删除真实券商环境变量并把共享凭据路径换成测试专用空路径，使测试模块导入和它启动的子进程都无法读取真实 OKX/Longbridge 配置。正式设置按测试隔离，测试需要的大模型 Key 显式使用测试值。

- 2026-07-24：GUI 当前可编辑的风险配置，不等于已经运行的 Campaign 实际参数。Campaign 启动时必须把资金上限、风险比例和最大杠杆与配置指纹一起冻结到状态文件；GUI 同时显示“GUI 配置”和“实际冻结参数”，二者不一致、状态陈旧或旧状态缺字段时必须标红为未知/不一致。保存失败时要保持磁盘、共享内存和服务全部仍为旧值；只有成功落盘后才允许原子更新进程内配置。

- 2026-07-24：用户切换 VPN 节点会改变公网 IP；即使 Worker 服务进程仍在，OKX `50110 / IP 不在 API Key 白名单` 也会让所有私有读取失效，不能再称 Worker 健康或账户空仓。系统必须保持停写，GUI 要显示真实错误；用户恢复白名单后，先重新取得仓位、普通单、全部算法单和账户身份真值，再走专用临时读取故障恢复，禁止用普通 CLEAR 或重锚高水位。Campaign 进程是否退出要由进程和状态文件证明，不能仅按故障时间推断。

- 2026-07-24：多个 Codex 任务共享同一 PA_Agent 工作区时，只能有一个明确总控拥有写代码、跑测试、操作 Worker 和 Campaign 的权限。发生交接时，原任务最多完成双方确认的一个原子改动并立即停写；用户指定新总控后，冲突任务必须关闭，不能继续以“补充修复”为由覆盖已验证代码或反复要求重启生产服务。

- 2026-07-24：用户已把风险定仓改为“可配置固定资本上限 × 可配置风险比例”，实际有效资本取 `min(用户上限, 最新 USDT 权益)`；账户权益高于上限时，余额变化只更新审计，不能改变风险预算或让旧计划无故失效，权益低于上限时仍按真实余额自动收缩。杠杆只解决保证金容量，不改变止损风险数量；所需杠杆超过用户上限就硬阻断。PA 二元决策后的定仓、调杠杆、入场、原生止盈止损、同向持有和反向先离场再开仓属于确定性脚本，监控智能体只处理异常，不参与常规批准，也没有券商写权限。

- 2026-07-24：监督看到的风险数量必须是 Worker 最终能够提交的数量，不是参考值。提交前账户刷新若导致数量发生任何增减，旧监督授权立即作废并阻断，不能由 Worker 静默改量；回撤用账户总权益，XAU-USDT-SWAP 的 10% 风险定仓用结算币 USDT 权益，两种权益用途不得混写。监督快照要同时保存信号价、最终有效限价、止损、权益、预算和数量。

- 2026-07-24：Campaign 重启不能把 `last_completed_bar_ms` 清空，否则会重复分析甚至重复交易同一根已收盘 K 线。异常清理也不能只看一次状态：撤进入场单期间若发生部分成交，必须继续观察账本并切换为一次主动离场；未知状态仍禁止盲重试。

- 2026-07-24：OKX 永续最大可开量不保证随杠杆单调增加，跨风险档位时可能下降。动态杠杆只能逐点读取真实容量；发现非单调继续硬阻断，并把触发的前后杠杆/容量点耐久写入错误证据，不能用“更高杠杆容量更大”的直觉猜测。

- 2026-07-24：`limit_with_slippage` 必须同时保存“PA 信号价”和“适配器最终有效限价”。受控验收若要让最终限价落到实时买一/卖一，应先反推信号价，再由适配器唯一地应用一次 ATR；不能把实时价直接写成信号价后再加一次 ATR。监督快照、容量查询和 10% 风险定仓必须使用最终有效限价，委托计划仍保存原始信号价，二者都要可追溯。

- 2026-07-24：Demo 生命周期 runner 超时不等于券商交易失败。退出限价单可能在 runner 的旧 120 秒窗口后才完成；异常收口不能只入队减险命令就立即写状态，而应等待 Worker 把 `canceled/closed` 终态落到账本。Campaign 还要在后续只读监控中把较新的 execution 终态同步到状态显示，避免券商和执行账本已 CLOSED、界面仍显示 `exit_pending`。

- 2026-07-24：Codex CLI 候选存在但版本探测超时，不能报成“未安装”，也不能当永久模型故障让 Campaign 退出。缺少候选、临时探测超时、启动或版本明确失败必须分开；临时超时属于当前 K 线的网络/资源错误，耐久记录后继续下一根，成功探测的绝对路径可以缓存，但真实登录和模型调用仍须每次验证。

- 2026-07-24：提交前按最新权益重新定仓时，不能只更新 `ExecutionPlan.quantity`。`ExecutionRecord.remaining_quantity` 在订单受理、首次对账和异常重启之间也是耐久真值；若仍保留旧计划数量，GUI 与恢复逻辑会短暂显示不可能的剩余仓位。风险定仓事件落盘时必须原子同步这两个字段，后续成交回读再按券商真值更新。

- 2026-07-24：Worker 健康判定不能把正常待领取的 `PENDING` 写命令归为“未解决券商写入”。周期对账与 Controller 入队并发时，这会让 Worker 自己进入 `NEEDS_ATTENTION`、撤销合法租约并在真正领取前杀掉提交。只有崩溃遗留的 `RUNNING` 和没有耐久裁决的 `UNCERTAIN` 才应阻断可用态；`PENDING` 必须保留租约并由下一次 `run_once()` 正常领取。验收必须覆盖“对账结束与 submit 入队交错”的真实时序，不能只测串行命令。

- 2026-07-24：不能把“Campaign 进程正常、每根 K 线记录 no_order”当作持续自动交易已经履责。PA_Agent 开发期间必须同时维持 Worker、Campaign、券商只读监控和真实 Demo 执行反馈；有效自然信号立即走真实监督与 Controller→Worker 完整闭环。只要本阶段修改或验收执行链，自然信号持续 no_order 时也不能空等到阶段结束，应及时使用明确标记、真实行情与容量、真实 SupervisorGate 的 `controlled_reproducible` 闭环取得交易反馈，并与自然策略绩效严格分开。复杂阶段要并行安排只读监控子 Agent，结束后再做对抗审查；不能让离线开发、测试或审查挤掉交易运行。

- 2026-07-24：网页版 GPT/BioMNI 工单只能作为目标输入，不能替代本机 skills、memory、Git、进程、数据库和券商现场。开工后先把工单逐项映射到当前真值；已由并发任务真实完成的项直接验真并收口，剩余项沿生产主链猛推。不得用重复规划、泛化风险清单或“等全部离线工作做完”拖延可立即取得的真实结果。

- 2026-07-24：不能因为核心执行代码和真实 Demo 已推送，就宣称整批工单远程闭合。发布前必须把文档中的每一条“已实现/已测试”主张反查到 staged 文件和远程 HEAD；若实现或硬测试仍只在工作区，必须精确补交或撤回主张，不能用核心链已完成掩盖发布漏件。

- 2026-07-14：用户要求安装、配置现有开源项目时，先保持上游源码原样；未经明确要求，不扩展实盘能力或主动改造安全实现。必须区分“运行时配置”和“修改项目代码”，披露原项目限制后停在必要决策点。
- 2026-07-15：用户说“参考其他项目的前端优化”时，指复用“网页版 GPT 独立审查 → Google Stitch MCP 生成/迭代设计 → 人工验收 → 再落地代码”的流程，不是要求 PA_Agent 复制参考项目的视觉风格。
- 2026-07-16：PA_Agent 的首轮 Stitch 方案视觉完成度不足。后续前端重设计应先拆解 `D:\Desktop\Quant\前端设计\长桥` 的真实页面素材，提炼长桥式的留白、层级、卡片/表格密度、导航与行情配色，再把明确视觉规则交给 GPT/Stitch；不能只写“参考长桥”或在现有 QSS 上换颜色。
- 2026-07-16：对已有产品做 Stitch 重设计时，必须先截取当前产品全部关键界面，并从代码整理“页面—功能—状态—交互—不可丢失契约”；把这些现状材料作为产品真源提交给 Stitch，外部产品截图只能作为视觉语言参考。不得只给外部参考图就让 Stitch 猜测现有产品功能。
- 2026-07-16：用户说“目前在 OKX 里只做 XAUUSD”时，是要求优先完成黄金的现货类产品与永续合约交易链路，同时保留后续扩展其他品种的架构；不能曲解为永久硬拒绝所有非黄金品种。
- 2026-07-16：PA_Agent 与 AlphaMaster 虽然都用于量化投资，但属于两个完全独立的项目。上下文、Git 提交、验证、文件包、模型任务和未来架构必须分别处理；未经用户另行明确授权，不得设计共享执行层、跨项目信号接入或统一交付任务。
- 2026-07-16：网页版 GPT 通过 GitHub 给出的方案看不到本机 Codex 的 skills、memory、未提交状态、进程和私有配置，因此只能作为外部规划输入。正式执行前必须由本地 Codex 按当前工作区和 `docs/LOCAL_EXECUTION_CONTEXT.md` 校正模块路径、工具能力、命令、验证方式与安全边界，不能直接照搬。
- 2026-07-16：PA_Agent 实盘目标不是增加一次性“下单 API”，而是把 PA 分析结果中的入场、止盈、止损等指令接入 Longbridge 与 OKX，覆盖订单受理/成交、持仓监控、止盈止损或主动离场、账户资金与已实现/未实现盈亏读取，并把状态回写 PA。验收必须证明这条完整生命周期；同时只实现该闭环所需的最小模块，不扩张为通用交易平台。
- 2026-07-16：第三方平台开发者已明确确认某项集成当前不可用时，应停止重复授权和连通性测试，把该限制记录为外部边界；不得继续把问题归因于用户权限配置。
- 2026-07-17：大模型供应商首轮接入必须收敛到用户明确点名的 Codex 订阅、Kimi API、DeepSeek API 和小米 MiMo API；PA_Agent 复用现有模型档案，不为“以后可能支持”重写通用框架。验收要区分“请求已正确到达供应商”和“账户有额度并生成回复”，不能把供应商余额不足误判为代码故障。
- 2026-07-17：大模型适配器存在于后端、下拉框里能选，不等于前后端已经对用户可用。必须把用户要用的档案实际保存到配置、逐个测试并让前端列表可见；否则应明确说“后端已接入、前端尚未完成配置”，不能笼统宣称已经统一。
- 2026-07-17：订阅制档案显示“已测试”仍不等于订阅链路可用。必须实际覆盖“无 API Key 激活档案 → 主界面不再弹 API Key 门禁 → 保存并测试 → 发起一次真实分析”完整路径；订阅登录状态、网页登录/设备码登录入口和 API Key 配置必须在界面与后端语义上彻底分开。
- 2026-07-17：排查“极度激进没有下单”时，必须同时核对模型原始阶段二输出、归一化后的最终决策和执行账本。模型已经给出交易方案却被程序规则改成不下单时，必须报告为“程序拦截”，不能误报为“模型没有产生下单决策”。
- 2026-07-17：PA 前端改版必须先把当前主界面、模型设置和实盘生命周期作为功能真源交给 Gemini Stitch；OmicOS 只参考供应商分组和信息层级，不能照抄品牌风格。Codex 不直接凭审美改界面，必须先验收 Stitch 设计没有删除交易能力，再落 PyQt6。
- 2026-07-17：模型上下文上限是模型能力元数据，不是普通档案的自由输入项；累计多次请求的 Token 用量也不是当前请求的上下文占用。界面应只读展示可靠上限或“尚未确认”，并把累计用量与单次请求上下文分开。
- 2026-07-18：模型自动拉取成功后，账号接口返回值才是可选真值；内置目录只用于首次加载、失败兜底和为相同模型补能力元数据，不能把账号未返回的旧模型混入下拉框。刷新失败必须保留原列表，模型能力变化必须使旧测试失效。
- 2026-07-18：API Key 的“显示”状态不能跨供应商或档案继承；切换后必须立即重新遮罩。模型目录后台结果还必须绑定适配器、地址和 API Key 指纹，防止旧账号的迟到结果覆盖新账号界面。
- 2026-07-18：自动测试必须用正式 `config/settings.json` 的前后 SHA-256 作为硬隔离门禁，并把默认保存路径延迟到运行时解析；测试曾短暂写到正式配置时，即使随后恢复，也不能把该轮测试视为安全通过。
- 2026-07-19：Codex 能拉取真实模型列表，只证明“模型目录”可用，不证明订阅登录、真实回复、档案保存、运行时激活和重启恢复已经贯通。以后只有在真实 GUI 链路完成“检测登录 → 测试并保存 → 激活 → 重启读取”且全程不要求 API Key、不关闭重开设置窗口后，才能宣布 Codex 订阅接入完成。
- 2026-07-20：Codex 断线重连后，排队消息可能越过尚未完成的前置任务。恢复时不能按聊天顺序假定已经完成，必须重新建立“代码存在 / 自动测试 / 真实外部调用 / 用户可见界面 / 生产配置”五层完成矩阵；任何一层缺失都要明确标为部分完成或未完成。
- 2026-07-20：交易系统设计不能先假定必须自己实现。先检索小红书、微信、抖音/B站等真实用户社区中的故障，再按“成熟开源社区工具 > 小范围高质量工具 > 必要的项目内适配层”核验许可证、代码、测试和真实边界。商业闭源产品只借鉴工程分层，开源项目只有在代码语义与 PA 的账户、订单、恢复和风险边界一致时才复用；明星数或宣传文章不能替代代码级审查。
- 2026-07-20：`needs_attention` 只是异常现象，不等于写权限类别。新增风险租约、减险写入和持久停写必须分别建模；券商明确拒绝后还要结束旧运行意图，否则重启会把“确定没写入”误判成“结果未知”，造成永久停写。验收必须覆盖“拒绝 → 重启 → 无写只读对账 → 清标 → 下一次人工操作”完整链。
- 2026-07-20：用户修改 GitHub 用户名后，不能依赖旧远端重定向或旧“私有仓库”记忆。任何提交/推送前都要实时核对登录账号、新仓库地址、可见性、权限和远端 SHA；当前 `xiaojinlucky/PA_Agent` 若仍为公开仓库，就必须停止推送。
- 2026-07-20：大模型档案“真实测试通过”后激活失败，不应只排查登录或模型 ID，还必须核对共享 `settings.json` 的 revision。Windows 文件锁只能可靠约束外部进程，PA 同一进程内的并发线程还需要独立线程锁。行情/周期等非 AI 设置在测试期间保存时，应在同一临界区保留磁盘最新普通设置并只合并本次已验证 AI 档案；只有另一窗口也修改 AI 档案时才拒绝覆盖。验收必须覆盖“Sol 活动 → Terra 测试通过 → 普通设置并发保存 → 激活 → 重载仍为 Terra”以及“合并读取后第二线程不能插入写入”。
- 2026-07-20：Windows 提权脚本不能只用当前 PowerShell 7 验证。实际 UAC 会调用 Windows PowerShell 5.1；隐藏、非交互宿主可能不支持 `Start-Transcript`，5.1 也不能解析依赖 PowerShell 7 自动续行的换行管道。提权前必须先用目标 `powershell.exe` 做只解析检查，日志写普通结果文件，成功后再复扫实际 ACL 和服务进程树，不能凭管理员进程启动就宣称完成。
- 2026-07-20：不能在 `PA_AGENT_PAPER_TRADING_ENABLED=false` 或服务尚未重新加载该值时，把 Longbridge/OKX 模拟链路描述成“界面可直接启用”。交付前必须同时核对共享 env、WinSW 进程环境、Worker 最近成功对账和全部活动 execution；若历史 Demo 仓位未安全收口，应先只读确认订单/保护真值，再显式减仓离场，不能为消除弹窗直接放开新风险。
- 2026-07-23：5 分钟自动循环正在运行，不等于每根 K 线都会产生订单。向用户报告时必须把“循环活着、AI 已分析、AI 决策不下单、计划被程序拦截、订单已提交、订单成交或撤销”分开写；用户在券商账户看不到后续订单时，先核对每根 K 线的原始阶段二决策、归一化结果、计划构建和券商账本，不能只说“后台还在跑”。
- 2026-07-23：为快速验收订单链路而把一个远离现价的限价计划直接改成市价单，会让原止损/止盈相对实际成交价失真。正确做法是只在运行器专用提示中要求模型按最新 K1 附近重新构造完整三价；若 §14 仍判定无合法即时交易，应报告为策略不下单，而不是暗中放宽硬性禁止项制造成交。
- 2026-07-23：当用户要尽快看到完整订单生命周期、但真实策略连续判定“不下单”时，必须把“策略交易”和“Demo 技术闭环验收”拆开。验收单要明确标记、固定 Demo 路由和数量、只经现有 Controller/Worker，且不能被后续策略循环误读为真实分析；这样既能快速验证入场、保护和离场，也不污染策略效果判断。
- 2026-07-23：用户明确要求多周期必须结合真实 PA 原始资料推进。首轮按“高周期背景标签实验 → 极薄 Session 标签 → 成交量维持现状”推进；`15m` 主周期先接 `1h` 和 `4h`，高周期只解释嵌套趋势/区间/位置，不能机械否决主周期，也不能把所有周期整表塞进模型。正式实现前先核对用户提供的原始资料和识别文本，再把事实映射到提示词字段、代码路径和硬验收。
- 2026-07-23：用户要求“限价单加滑点”必须随当前波动变化，不能固定基点。当前执行语义是分析节点捕获的主周期 `ATR14` 乘以可配置倍数；缺 ATR 就直接阻断，入场与主动离场仍分别选择 `limit`、`limit_with_slippage`、`market`，原生止盈止损保护与主动离场方式分开验收。
- 2026-07-24：用户把现役主周期、阈值和倾向正式改为 `10m / 20 / extreme_aggressive`。OKX 没有原生 10m 时，只能用两根连续、同一 UTC 10 分钟边界且均 `confirm=1` 的官方 5m K 线严格聚合；缺根、乱序、重复、跨边界或未收盘必须明确失败。高周期继续用可真实获取的 1h/4h 薄背景，不能伪造 40m/160m，也不能机械否决主周期。
- 2026-07-24：`controlled_reproducible` 只表示工单已授权受控输入，不等于监督自动放行。受控记录的操作性阶段一必须与方向、三价、真实 K 线、ATR 和风险容量一致；自然分析原文只能保留在 `base_*` 审计字段。SupervisorGate 仍须真实模型判断并可因结构、价格或风险不合法而阻断。
- 2026-07-24：Worker 进程启动后不会自动加载磁盘上的新 Pydantic schema。新增执行计划字段后，任何真实写入前必须核对 WinSW Worker 的启动时间/PID并安全重启；券商调用前的本地记录解析失败应明确标为 `failed:execution_record_invalid`，只读确认没有订单、成交或仓位后才能经 Controller 作废 READY，不能把旧 `uncertain` 命令直接盲重提。
- 2026-07-24：自动离场期间 Worker 可能因单次全局对账异常短暂进入 `needs_attention`，随后在持久恢复循环中完成离场。调用方不能只看一次心跳就把生命周期写成失败，必须优先重读 execution 和券商持仓；只有 execution 自身 `needs_attention/unknown/error` 或超时仍未闭合时才硬阻断。最终完成仍必须证明入场成交、原生保护、主动离场、`CLOSED`、空仓和空挂单。
- 2026-07-24：账户 USDT 或总权益突然变化时，必须把“交易风险容量刷新”和“资金流对交易盈亏/回撤基准的校正”分开处理。余额变化本身不产生交易信号；下一笔有效信号使用最新权益、真实止损距离和 OKX 实时最大可开量重新计算 10% 风险数量，不沿用旧数量、不截断冒充通过。回撤校正必须使用账户总权益而不是单一 USDT 币种权益，并逐页核对 OKX 资金流水，把转入、转出、充值、提现和账户划转排除出交易盈亏及历史高水位；内部换币不是外部入金。流水不完整或分类不明时保留最后可信水位并告警，不能猜测。只有只读分页与纯计算、没有生产账本游标/高水位和恢复接线时，必须明确标为阶段 4 地基而不是完整能力。
- 2026-07-24：风险定仓通过账户最大可开量，不等于单笔市价订单一定低于 OKX 的 `maxMktSz`。市价入场必须在预检阶段完整阻断超限计划；市价主动离场属于减险，可以按实时单笔上限拆分并逐笔回读成交，但不能改写或截断原始总持仓、风险数量和已实现盈亏。
- 2026-07-24：真实交易最终数量与风险公式吻合，不等于定仓证据已经完整。每次候选进入监督和计划前，必须把当时权益、10% 预算、入场/止损、合约价值、费率、保守滑点、`minSz/lotSz`、方向容量 `maxBuy/maxSell`、风险占用和目标数量写进同一耐久策略记录；成交后快照可以用于明确标注的账本重建，但不能冒充入场前直接快照。
- 2026-07-24：Campaign 配置切换不能只因状态中存在历史 execution ID 就永久阻断，也不能只看列表为空。必须逐个回查耐久执行账本：只有 `closed/canceled/rejected` 才允许归档旧 Campaign；记录缺失、活动态、`unknown/error` 都继续阻断，且切换前仍须验证券商空仓、空单、Worker 健康和无租约。
- 2026-07-24：券商部分成交量通常是该订单的累计值，不能每轮都从上轮剩余仓位重复扣减。主动离场意图必须耐久保存“该订单提交前的剩余仓位”，每轮只计算“固定基线减累计成交”；升级前在途单要用上一轮剩余量、上一轮累计成交量和订单数量保守恢复基线。
- 2026-07-24：未知保护撤单不能盲重发，但也不能在券商已经明确显示原单仍 `live` 时永久停住。正确恢复是先只读确认同一算法单仍有效且没有触发子单，耐久生成新的撤单意图并清除未知标记，下一轮再次复核后才由唯一 ExecutionWorker 发出精确撤单。
- 2026-07-24：OKX 算法保护单撤销成功后，单笔详情接口可能返回 `51603 / Order does not exist`，不能直接当读取故障永久卡住，也不能把“曾确认存在的算法单”复用为“从未创建”的 `confirmed_absent`。必须按客户算法订单号完整查询待处理单和 `effective/canceled/order_failed` 历史，保留所有既有子单与成交证据；历史仍查无时，OPEN 状态直接停写，主动离场只在无历史成交证据、普通单和八类算法单均空、真实同向仓位数量与账本一致时继续。对这种查无恢复，最终离场 POST 前还要在行情和订单参数构建完成后紧贴写入点再次复核，任何变化或读取失败都必须 0 写入。
- 2026-07-24：用户确认“其他资产换成 USDT/账户内转换”时，必须按 OKX `type=2/subType=1` 的内部换币处理，不能当成外部入金、交易盈利或资金流高水位调整；同时仍要让下一次定仓读取最新 USDT `eq`。真实验收要同时证明账单分类、余额快照刷新、风险基数语义和提交前旧计划失效。
- 2026-07-24：资金流风险账本第一次建立可信基线时，当前 `account.totalEq` 已经包含此前所有持仓、盈亏和历史转账；此时只能校验账单边界并把当前总权益设为高水位，不能再把边界前历史转账重复加减。只有持久化扫描边界之后的新账单才做外部资金流分类。旧版本在首次启动就因历史 BTC/ETH 转入触发 `unsupported_transfer_currency` 且从未保存权益、高水位和扫描时间时，可以重新建立首个可信基线；已经建立过可信高水位或扫描边界的状态绝不能借此自动重置或解除真实回撤停机。
- 2026-07-28：本机 Chrome 扩展和 Native Messaging Host 均正常、简单页面导航却固定多耗时约 20 秒时，先分别验证 `curl`、Node 默认 `fetch` 与启用环境代理后的 `fetch`。若外网直连超时、代理请求正常，而 Node 默认请求约 10 秒失败，应判定为浏览器控制服务未继承代理；设置用户级 `NODE_USE_ENV_PROXY=1` 后重启 Codex 再复测，不能反复重装扩展、切换浏览器或归因于页面复杂度。
- 2026-07-29：用户决定把视觉设计交给其他大模型时，应立即把浏览器、Stitch 和图像生成流程降为历史，不再拿工具故障阻塞前端。先补齐不依赖视觉的只读后端合同和异步边界，再交付一份自包含、唯一真值的 PRD；旧 PRD、失败工具记录和候选稿不能继续作为并行真值。
