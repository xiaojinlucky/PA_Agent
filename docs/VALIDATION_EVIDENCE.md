# PA_Agent 当前验证证据

> 本文件只保留当前仍有用、且可公开复核的结论。旧机器地址、进程号、
> 账户快照、订单编号、会话地址和临时运行细节已从当前文档移除；需要追溯时
> 使用 Git 历史。机器可读真值见
> `docs/evidence/capability-index.json`。

## 证据口径

- **代码存在**：只说明实现已经进入仓库。
- **测试通过**：只说明确定性测试覆盖的行为通过。
- **外部只读验证**：说明真实数据源读取成功，不等于交易可用。
- **离屏界面验证**：说明固定夹具下的界面结构、状态和尺寸通过，不等于真实桌面验收。
- **运行态验证**：必须来自当前进程、当前完整 Git SHA、当前数据库版本和新鲜券商只读事实。
- **稳定发布**：只有能力索引所有适用层均为 `verified`，且
  `stable_release_ready=true`，才允许创建稳定 tag 和 GitHub Release。

## 当前发布基线

- 发布目标：`v0.1.0`，Windows + Python 3.12 源码部署版。
- 明确不是：EXE、安装器、PyPI wheel、OKX Live 或 Longbridge 交易产品。
- 旧 OKX Demo 安全工作台保留；新多市场页只读。
- 交易默认关闭；成交量仍只做影子记录，不进入提示词、信号、仓位或风险闸门。
- 最新已验证的阶段 C 文档提交：
  `2506e2f365ab1af9cf1ffd42c0e70cb3ddb59368`。
- 该提交的 [GitHub CI](https://github.com/xiaojinlucky/PA_Agent/actions/runs/30492884967)
  已通过。

## P0-01：一次性新增风险授权

- 实现提交：`c932e0113e9c4e33771d1cc5afc1f16beda46421`。
- Worker schema v5 把一个非空新增风险租约耐久绑定到唯一命令。
- 绑定租约与插入命令位于同一个 SQLite 事务；任一步失败会整体回滚。
- 数据库、Controller 和 Worker 分别核验唯一消费者。
- 并发线程、并发进程、调用者崩溃、插入失败、错误命令、过期边界、
  UNCERTAIN、跨动作复用、续租竞态和 v4→v5 异常迁移均有直接回归。
- 提交级确定性 CI：2048 项通过、0 失败、0 错误；存在 1 项既有宿主机条件跳过。
- **仍未完成**：运行中的旧 Worker 尚未安全重载，因此当前生产控制库仍未证明已加载 v5。

## B1：Longbridge 只读行情合同

- 实现提交：`1b0f6c9eacd54326975fd11ba8cb86e78a4b1daf`。
- AAPL.US、700.HK、600519.SH 已通过真实 Longbridge 只读验证。
- 报价权限由服务端证据产生；调用方不能自报实时或延迟。
- 报价和 10m/1h/4h K 线使用同一分析截止时间。
- 10m 是硬输入；1h/4h 可缺失，缺失不会终止合格的 10m 分析。
- 重复标的、缺失标的、额外标的和同时间戳内容冲突均失败关闭。
- 无时区时间已覆盖 UTC、上海、东京宿主机和美股夏令时边界。
- 港股半日市、A 股午休、乱序和重复数据均有测试。
- 三只股票都没有取得可追溯的权威最小价格变动单位，因此股票只能展示，
  价格分析明确不可用；没有运行股票两阶段 AI。
- 提交级确定性 CI：2082 项通过、0 失败、0 错误；存在 1 项既有宿主机条件跳过。

## B2：多市场 Controller

- 实现提交：`18951dc53a5d2b075bda0759676a68dd62dca172`。
- `MarketWorkspaceController` 是选择版本、自选版本、请求序号、数据来源、
  分析截止时间、设置保存和分析状态的唯一所有者。
- 它不导入 Qt，也不导入或调用交易执行层。
- 逆序回调、快速切市场、重复刷新、保存迟到、保存失败、认证恢复和分析中切换均有直接回归。
- 提交级确定性 CI：2137 项通过、0 失败、0 错误；存在 1 项既有宿主机条件跳过。

## C：原生 PyQt6 多市场页

- 实现提交：`91715ee2c8af0e6e829031b5a4f375d18f71cf52`。
- 新页面覆盖加密、美股、港股和 A 股，只消费 Controller。
- 页面没有交易按钮，也不导入或调用执行层；旧 OKX Demo 工作台仍可达。
- 10m 为主分析周期；1h/4h 只作为可缺失背景。
- Longbridge 没有权威最小价格变动单位时仍可看盘，但分析按钮关闭。
- 本地确定性全量：2164 项通过、0 失败、0 错误、0 跳过。
- 提交级确定性 CI：2164 项通过、0 失败、0 错误；存在 1 项既有宿主机条件跳过。
- 已生成并逐张检查 16 份离屏证据：两种窗口尺寸、九种主要状态，以及
  100%/125%/150% 缩放矩阵；16 份通过、0 失败。
- **仍未完成**：离屏夹具不读取真实网络、数据库或桌面环境，不能替代用户从正式快捷方式完成的真实桌面矩阵。

## D：源码部署与发布证据

- 发布链主体已由 `4c9d16fe29611a2c35eaa725e2e843d2e745885b` 提交并推送：
  - 单一 `0.1.0` 版本真值；
  - 不启动 Qt、网络或数据库的 `--version` 与 `--self-check`；
  - Windows 安装、卸载和快捷方式脚本；
  - 源码 ZIP 的敏感文件拒绝清单；
  - 五层能力索引、脱敏样例、Release 清单和哈希；
  - Release 工作流和干净 Python 3.12 源码安装门；
  - 16 份离屏证据的图片解码、尺寸、缩放、运行态零读取和完整 SHA 校验。
- 该 SHA 的 GitHub Actions run `30502065534` 在首个界面冒烟用例后因
  pyqtgraph/Qt 坐标轴销毁竞态硬崩；没有 pytest 断言失败，JUnit 未生成，
  工作流按设计失败，候选 workflow 未启动。
- 图表重复关闭、主窗口显式关闭和工作区直接销毁 3 条生命周期回归已先红后绿。
  相关界面套件连续 5 轮各 43 项通过；最终非 live 2227 项通过，
  失败/错误/跳过均为 0；CI 与候选 workflow 的测试数量门已同步为 2227。
- Qt 生命周期修复提交 `ed8c52ee61fa2ff379b561f554774c3ac4da619e`
  的 CI run `30504823117` 已完整跑到 100%，没有再出现 Qt 底层访问冲突；
  远端结果为 2223 项通过、3 项失败、1 项宿主机条件跳过。3 项失败都来自
  Windows PowerShell 5.1 将无 BOM 的中文发布脚本按旧编码读取。
- 三份发布脚本已补齐 UTF-8 BOM，并由现有测试强制该字节合同。Windows
  PowerShell 5.1 与 PowerShell 7 均可解析；发布/离线/E2E 定向 61 项通过，
  最终非 live 2227 项通过、失败/错误/跳过均为 0。
- 独立只读对抗审查未发现 P0/P1：本轮没有改执行逻辑、没有把测试切到
  PowerShell 7，也没有提前宣称提交级 CI、干净安装或稳定发布已经通过。
- 两个入口的离线自检均通过：Python 3.12、37 个提示词资源、两个入口、
  固定 Git 依赖和交易默认关闭全部满足。
- 敏感 manifest 附加字段并重算校验和的红色反例先真实失败；严格字段合同、
  私有路径扫描和待上传集合密钥扫描实现后转绿。
- 该阶段只有在 PowerShell 5.1 编码修复对应新 SHA 的提交级 CI、提交后
  `git archive`、全新目录安装和
  发布包扫描全部通过后，才能把状态改为 `verified`。

## 当前稳定发布阻塞

1. 固定 OKX Demo 网络通道不可用，无法取得新鲜私有只读真相；因此禁止重载 Worker、
   迁移生产控制库、解除风险停止或运行 Demo 写闭环。
2. 正式快捷方式的 Longbridge/四市场桌面矩阵尚未由用户验收。
3. 阶段 D 尚未完成 PowerShell 5.1 编码修复对应新 SHA 的提交级 CI、提交后干净源码安装和最终发布包校验。

以上任一项未关闭时，只能生成候选证据，不得创建 `v0.1.0` 稳定 tag，
不得发布稳定 GitHub Release，也不得声称项目已全部完成。
