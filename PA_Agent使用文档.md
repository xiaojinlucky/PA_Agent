# PA Agent — 功能详细使用文档

> AI K线分析辅助工具，基于价格行为学（Price Action），通过 DeepSeek / Claude 等大模型进行市场结构分析与交易决策。

---

## 目录

1. [启动与初始配置](#1-启动与初始配置)
2. [主界面概览](#2-主界面概览)
3. [实时K线图表](#3-实时k线图表)
4. [提交分析（核心功能）](#4-提交分析核心功能)
5. [增量分析](#5-增量分析)
6. [等待K线收盘后分析](#6-等待k线收盘后分析)
7. [两阶段分析流程详解](#7-两阶段分析流程详解)
8. [AI 侧边栏七个标签页](#8-ai-侧边栏七个标签页)
9. [决策面板详解](#9-决策面板详解)
10. [决策树与决策树可视化](#10-决策树与决策树可视化)
11. [分析后自由对话（Free Chat）](#11-分析后自由对话free-chat)
12. [演示模式](#12-演示模式)
13. [设置对话框](#13-设置对话框)
14. [交易倾向（Decision Stance）](#14-交易倾向decision-stance)
15. [策略文件路由机制](#15-策略文件路由机制)
16. [数据源与K线刷新](#16-数据源与k线刷新)
17. [经验库系统](#17-经验库系统)
18. [记录持久化](#18-记录持久化)
19. [安全与密钥管理](#19-安全与密钥管理)
20. [常见问题与故障排除](#20-常见问题与故障排除)

---

## 1. 启动与初始配置

### 启动方式

```bash
# 方式一：直接运行
python run.py

# 方式二：模块方式运行
python -m pa_agent.main

# 方式三：安装后使用命令行入口
pip install -e ".[dev]"
pa-agent
```

### 首次运行

1. 程序启动后自动创建 `config/settings.json`（默认配置）
2. 使用 MT5 时，如提示未连接，确保 MetaTrader 5 终端已打开并登录
3. 首次使用需打开 **AI 模型设置**，填写适配器、模型、Base URL 与 API Key，并点击“测试并保存”
4. API Key 输入框默认隐藏；当前配置格式仍会明文写入被 Git 忽略的 `config/settings.json`，请勿共享该文件

### 环境要求


| 项目     | 要求                                   |
| ------ | ------------------------------------ |
| 操作系统   | Windows 10/11                        |
| Python | 3.11+                                |
| MT5 终端 | 需运行并登录（用于获取实时行情）                     |
| Longbridge | 可选；需在 `Quant\env` 配置 OpenAPI 三项凭据          |
| 网络     | 需访问 AI API 服务（DeepSeek / PackyAPI 等） |


---

## 2. 主界面概览

程序主窗口分为**左侧图表区**和**右侧 AI 侧边栏**。顶部为单行操作控制栏，下面依次显示五阶段流程与五项摘要卡片。

```
┌──────────────────────────────────────────────────────────────────────┐
│ [数据来源▼] [交易所▼] [品种▼] [周期▼]  ☑等待收盘  [提交分析] [增量分析] [演示模式] │
├─────────────────────────────────┬────────────────────────────────────┤
│                                 │  ┌──────────────────────────────┐  │
│                                 │  │ 实时│决策树│可视化│决策│原始│调试│  │
│      K线图表（pyqtgraph）        │  ├──────────────────────────────┤  │
│      - 蜡烛图 + EMA20           │  │                              │  │
│      - 入场/止损/止盈线         │  │    AI 侧边栏内容             │  │
│      - 序号标签                 │  │    （按标签页切换）           │  │
│                                 │  │                              │  │
│                                 │  └──────────────────────────────┘  │
├─────────────────────────────────┴────────────────────────────────────┤
│ 状态栏：就绪 / 分析中 / 等待收盘…                                     │
└──────────────────────────────────────────────────────────────────────┘
```

### 控制栏元素


| 控件              | 功能                                             |
| --------------- | ---------------------------------------------- |
| **品种**          | 可编辑下拉框；MT5 输入终端品种名，Longbridge 输入 `AAPL.US` 等带市场后缀的代码 |
| **周期**          | K线周期选择：1m / 5m / 15m / 1h / 4h / 1d            |
| **用于分析的 K 线数量** | 在 **设置 → 通用设置** 中配置（通常为 2–5000；Longbridge 最多 945），默认 100；主窗口不再显示该输入框 |
| **等待收盘**        | 勾选后，提交分析时先等待当前未收盘K线走完                          |
| **提交分析**        | 发起完整两阶段 AI 分析                                  |
| **增量分析**        | 基于上一次成功记录做增量分析（跳过已分析K线）                        |
| **演示模式**        | 回放已保存的分析记录，无需真实 API 调用                         |
| **图表实时更新**      | 分析进行中图表暂停刷新，点击此按钮可恢复                           |


---

## 3. 实时K线图表

### 图表特性

- **蜡烛图渲染**：使用 pyqtgraph，30Hz 刷新率
- **EMA20 叠加线**：自动计算并绘制 20 周期指数移动平均线
- **K线序号标签**：已收盘棒显示 #1、#3…（K1 = 最新已收盘）；实时更新时最右侧 **未收盘** 棒为 **浅色空心 K 线**，不参与 AI 分析
- **分析后少一根？** 提交分析后图表冻结为「仅已收盘」快照，未收盘棒会消失，与发给 AI 的 K1 对齐 — 详见 `[docs/图表K线与分析快照说明.md](docs/图表K线与分析快照说明.md)`
- **交易决策叠加**：分析完成后自动绘制入场价、止损价、TP1、TP2 水平线
  - 入场线：白色实线
  - 止损线：红色虚线
  - TP1 线：绿色虚线
  - TP2 线：浅绿色虚线
  - 方向标记：做多↑ / 做空↓

### 图表操作

- **鼠标滚轮**：缩放图表
- **鼠标拖拽**：平移图表
- 切换品种/周期时，图表自动重置

### 数据来源

控制栏 **「数据来源」** 可选：


| 来源               | 说明                                   |
| ---------------- | ------------------------------------ |
| **MT5**          | 需 MetaTrader 5 终端运行并登录（默认）           |
| **TradingView**  | tvDatafeed，现货黄金等                     |
| **Longbridge**   | 官方 OpenAPI 只读证券行情，不读取账户或执行下单          |


- 默认 1 秒刷新间隔（Longbridge 最低 2.5 秒，可在设置中调大）
- 数据源未连接时，状态栏会提示

---

## 4. 提交分析（核心功能）

### 操作步骤

1. 在控制栏选择 **品种**、**周期**（K 线数量在 **设置** 里配置）
2. 点击 **「提交分析」** 按钮
3. 图表自动冻结（暂停实时更新），状态栏显示"分析中…"
4. 右侧 AI 侧边栏切换到「实时」标签，展示分析流式输出
5. 分析完成后状态栏显示"分析完成"；若开启“分析完成后自动恢复图表”则恢复刷新，否则保持冻结，等待手动点击「图表实时更新」

### 分析期间行为

- 图表冻结在提交瞬间的K线快照上
- 「图表实时更新」按钮变为可用，可手动恢复图表刷新
- 提交按钮锁定，不可重复提交
- 品种/周期切换会取消当前分析

### 注意事项

- 确保品种名与 MT5「市场报价」中名称完全一致（含后缀如 `m`）
- 品种名输入错误时，输入框下方显示红色警告
- K线数据不足时提示"数据不足，请等待缓冲区填满后再提交"

---

## 5. 增量分析

### 什么是增量分析

当同品种同周期已有成功分析记录，且新增已收盘K线数量不超过阈值（默认 10 根）时，提交分析自动走增量模式：

- AI 可看到上一次分析的结论作为上下文
- 只对新增K线做增量判断，而非从零开始
- 节省 token 消耗，提升分析速度

### 操作方式

- **自动增量**：当新增K线 ≤ 阈值时，点「提交分析」自动走增量模式
- **强制增量**：点「增量分析」按钮，无视阈值限制，强制基于上一次记录做增量
- 阈值在 设置 → 通用 → 「增量分析最大新增K线」 中配置（0 = 关闭自动增量）

### 增量分析失败的情况

- 未找到该品种/周期的成功分析记录
- 当前K线与上一轮记录无法对齐（缺口过大）
- 此时应改用「提交分析」做完整分析

---

## 6. 等待K线收盘后分析

### 使用场景

当前最新K线尚未收盘（正在形成中），希望等它收盘后再提交分析，确保数据完整性。

### 操作步骤

1. 勾选 **「等待最新K线收盘后再提交分析」** 复选框
2. 输入框旁出现倒计时（如"还剩 23 秒"）
3. 点击「提交分析」或「增量分析」
4. 程序等待当前K线收盘，倒计时实时更新
5. K线收盘后自动提交分析

### 取消等待

- 取消勾选复选框 → 取消等待
- 等待期间状态栏显示"等待当前K线收盘…还剩 XX 秒"

---

## 7. 两阶段分析流程详解

PA Agent 的核心分析采用**两阶段（Two-Stage）架构**：

### 阶段一：市场诊断（Stage 1 — Diagnosis）

**目的**：识别当前市场结构、通道形态与周期位置。

**流程**：

1. 组装阶段一 Prompt（K线数据 + 市场诊断框架 + 人设提示词 + 逐棒分析检查单 + K线信号识别 + 经验库案例）
2. 调用 AI API，流式输出
3. 解析返回的 JSON，验证格式
4. 提取关键字段：
  - `cycle_position`：市场周期位置（尖峰/微型通道/窄通道/正常通道/宽通道/趋势型交易区间/交易区间/极端交易区间）
  - `direction`：方向（bullish / bearish / neutral）
  - `gate_trace`：闸门决策路径
  - `gate_result`：闸门结果（proceed / wait / unknown）
  - `detected_patterns`：检测到的形态（楔形/反转尝试/H1H2/突破失败等）

### 阶段二：交易决策（Stage 2 — Decision）

**目的**：基于阶段一诊断结果，给出具体交易建议。

**流程**：

1. 根据阶段一的 `cycle_position` + `direction` + `detected_patterns`，路由加载对应策略文件
2. 加载经验库中相关案例
3. 组装阶段二 Prompt（策略文件 + 决策树 + 交易倾向 + 经验库 + 二元决策指引）
4. 调用 AI API，流式输出
5. 解析返回的 JSON，验证格式
6. 提取关键字段：
  - `order_type`：下单类型（限价单/突破单/市价单/不下单）
  - `order_direction`：方向（做多/做空）
  - `entry_price`：入场价
  - `stop_loss_price`：止损价
  - `take_profit_price`：TP1（主止盈价）
  - `take_profit_price_2`：TP2（延伸止盈价）
  - `trade_confidence`：信心分数（0–100）
  - `decision_trace`：决策树追踪路径
  - `terminal.outcome`：最终结果（trade / wait / reject）

### 闸门短路

如果阶段一的 `gate_result` 为 `wait` 或 `unknown`，阶段二将被跳过（闸门短路），直接输出"不下单"结论。

---

## 8. AI 侧边栏七个标签页

右侧 AI 侧边栏包含七个标签页，按功能分类：

### 8.1 「实时」标签

**主要功能**：展示 AI 分析的流式实时输出。

- 分析进行时，实时显示思考过程（reasoning）和正文内容（content）
- 阶段一/阶段二分别显示，有清晰的标题分隔
- 底部显示 **Token 用量进度条**：
  - 绿色（< 80%）：正常
  - 黄色（80%–95%）：警告
  - 红色（> 95%）：危险
- 分析完成后，底部出现 **自由对话输入框**，可与 AI 继续讨论
- 支持「重发上一条消息」（自动丢弃已发送消息中的思考内容以节省 token）

### 8.2 「决策树」标签

**主要功能**：以文本列表形式展示决策树追踪路径。

- 显示闸门（Gate）追踪：每个闸门节点的问题、回答、结果
- 显示策略（Decision）追踪：每个决策节点的问题、回答、结果
- 终端结果标注：wait / reject / trade / proceed

### 8.3 「决策树可视化」标签

**主要功能**：以科幻风格分支流程图可视化决策路径。

- 节点分为「闸门」和「策略」两种类型
- 分支路径用线条连接，动画播放从根节点到终端节点
- 自动播放功能：分析完成后自动播放路径动画
- 支持缩放和拖拽查看
- 颜色编码：
  - 绿色 → 是/trade
  - 红色 → 否/reject
  - 黄色 → 中性/wait
  - 蓝色 → proceed

### 8.4 「决策」标签

**主要功能**：以结构化卡片展示最终交易决策。

- **市场概览**：趋势方向、周期位置、市场阶段
- **交易决策**：下单类型、方向、入场价、止损价、止盈价
- **信心评分**：AI 信心分数（0–100），带颜色进度条
- **交易者方程**：风险回报比、预估胜率、是否通过交易者方程
- **诊断摘要**：市场结构诊断的简要说明

### 8.5 「未来走势预期」标签

**主要功能**：展示下一根 K 线方向与下一个市场周期位置的结构化预期。

- 下一根 K 线：方向、概率与模型说明
- 下一个市场周期：候选位置、方向与概率
- 是否生成该模块由“其他通用设置”中的预测开关控制

### 8.6 「原始」标签

**主要功能**：展示 AI 请求和响应的原始数据。

- 阶段一/阶段二的 System Prompt、User Prompt 完整内容
- AI 原始响应（含 reasoning_content）
- 验证信息：JSON 解析是否通过、缺失字段、无效字段
- 异常信息：网络错误、超时、JSON 截断等
- 「复制调试信息」按钮：一键复制完整调试数据

### 8.7 「调试」标签

**主要功能**：展示本次分析使用的所有提示词文件。

- **阶段一文件列表**：市场诊断框架、人设、K线信号识别等
- **阶段二文件列表**：根据路由加载的策略文件
- **经验库条目数**：本次分析加载的历史案例数量

---

## 9. 决策面板详解

决策面板在「决策」标签中展示，包含以下信息块：

### 市场概览


| 字段   | 说明                                                     |
| ---- | ------------------------------------------------------ |
| 趋势方向 | 上涨 / 下跌 / 震荡                                           |
| 周期位置 | 尖峰 / 微型通道 / 窄通道 / 正常通道 / 宽通道 / 趋势型交易区间 / 交易区间 / 极端交易区间 |
| 市场阶段 | 稳定 / 过渡                                                |


### 交易决策


| 字段        | 说明                             |
| --------- | -------------------------------- |
| 下单类型      | 限价单 / 突破单 / 市价单 / 不下单          |
| 方向        | 做多 / 做空                        |
| 入场价       | 进场价格                           |
| 止损价       | 止损价格                           |
| TP1（止盈价） | 主止盈目标价格（用于 1.0–1.5R 交易者方程） |
| TP2（延伸价） | 延伸止盈目标价格（MM/更远结构，不参与方程）  |


### 信心与风控


| 字段    | 说明                            |
| ----- | ----------------------------- |
| 信心分数  | 0–100，>=70 绿色，50–69 黄色，<50 红色 |
| 风险回报比 | R:R（如 2.5:1）                  |
| 预估胜率  | 基于交易者方程计算                     |
| 交易者方程 | 通过 / 未通过                      |


---

## 10. 决策树与决策树可视化

### 决策树原理

PA Agent 内置一套**二元决策树**（基于 `prompt_engineering/二元决策.txt`），用于引导 AI 按固定逻辑链路做交易决策：

1. **闸门阶段（Gate）**：节点 0.1–0.3，判断市场是否具备交易条件
  - 0.1：是否有足够K线数据
  - 0.2：是否存在可识别的市场结构
  - 0.3：是否处于可交易环境（非铁丝网/极端混乱）
2. **策略阶段（Decision）**：节点 3–14，具体的交易逻辑决策
  - 入场信号识别（§9）
  - 入场方式选择（§11）
  - 止损止盈设定（§10.3）
  - 交易者方程验证
  - 硬性禁止项检查（§14）

### 可视化面板操作

- 分析完成后自动切换到此标签（如果设置了自动播放）
- 点击「播放路径」按钮手动播放
- 鼠标滚轮缩放，拖拽平移
- 设置中可配置默认缩放比例和播放时长

---

## 11. 分析后自由对话（Free Chat）

### 功能说明

两阶段分析完成后，可以在「实时」标签底部的输入框与 AI 继续对话：

- AI 已掌握本次分析的完整上下文（K线数据、诊断结果、决策过程）
- 每次发送消息时自动附加最新K线快照，确保 AI 看到最新市场数据
- 对话记录自动持久化到 `.followups.jsonl` 文件

### 使用方式

1. 分析完成后，输入框变为可用
2. 输入问题（如"如果明天跌破止损位怎么办？"）
3. AI 流式回复，实时显示
4. 可多次对话，上下文保持

### 注意事项

- 自由对话消耗额外 token
- Token 用量进度条实时更新
- 切换品种/周期会销毁当前对话会话

---

## 12. 演示模式

### 功能说明

使用已保存的分析记录（`records/pending/` 中的 `.json` 文件）回放界面，**无需真实 API 调用**。

### 启动方式

1. 点击「演示模式」按钮
2. 选择回放方式：
  - **手动选择记录**：弹出文件对话框，选择 `.json` 记录文件
  - **自动随机记录**：从 pending 目录随机选取记录

### 自动演示模式

- 自动模式下，每条记录回放完成后自动切换下一条
- 决策树可视化播放结束后自动切换到下一条记录
- 无可用记录时自动退出演示模式

### 退出演示

- 点击「退出演示模式」按钮（原「演示模式」按钮变为退出按钮）
- 提交真实分析时自动退出演示模式
- 切换品种/周期时自动退出

---

## 13. 设置对话框

菜单栏提供三个独立入口：**AI 模型设置**、**飞书发送通知设置**、**其他通用设置**。

### AI 模型档案

AI 模型窗口左侧是已保存档案，右侧是当前档案编辑区。窗口可缩放；低分辨率时编辑区会出现滚动条，底部“测试并保存 / 激活此档案”始终可见。

| 设置项 | 说明 |
|------|------|
| 档案名称 | 左侧列表中的名称，例如“DeepSeek 日常分析” |
| 协议适配 | 明确选择提供商请求语义；`自动识别`仅用于兼容旧配置 |
| 模型 ID | 上游服务实际接受的模型名称 |
| Base URL | OpenAI 兼容接口地址；Cursor Agent SDK 不使用该字段 |
| API Key | 默认以圆点隐藏，离开字段后会自动重新隐藏 |
| 上下文窗口 | 每个档案独立保存；按该模型官方文档填写，用于 Token 占用与预警，未知模型不自动猜测 |
| Thinking | 按适配器显示为可开关、固定开启或不支持独立控制 |
| 推理强度 | 仅显示该适配器原生支持的档位；没有通用强度参数时由模型决定 |

**档案操作流程**：

1. 点击“新建”或“复制”，填写档案名称和连接配置。
2. 点击“测试并保存”。程序在后台发起一次最短真实请求，不保存测试提示词或模型回复正文。
3. “连接 / 认证”“参数接受”“返回有效正文”和“准确回传本次随机挑战值”四项都通过后，档案才成为可激活状态。
4. “观察到 reasoning”只是观测信息：部分模型会深度推理但不返回可见推理文本，因此它不作为连接失败条件。
5. 点击“激活此档案”完成切换。切换时会重建客户端、终止旧自由追问会话并按新档案的上下文窗口重置 Token 统计，历史分析记录文件不受影响。

当前活动档案和唯一剩余档案不能删除。配置字段变化会使旧测试结果失效，必须重新测试。旧版单模型配置会自动迁移为“默认配置”；它可继续运行，但切换到其他档案前仍需完成测试。

**适配边界**：

| 适配器 | Thinking | 推理强度 / 速度 |
|--------|----------|-----------------|
| DeepSeek | 可开关 | 原生接受 `high` / `max`；界面较低档位会按官方语义归一化 |
| OpenAI | 依模型支持 | GPT-5 / o 系列按已知型号开放其原生档位；普通 GPT-4.x/4o 不发送 `reasoning_effort`；当前仅支持 Responses API 的 Pro/Codex 型号会在测试前明确拒绝 |
| Anthropic Adaptive | 可开关或部分模型固定开启 | 通过项目现有 OpenAI 兼容网关发送 adaptive / effort 语义，不是新增 Anthropic 原生客户端 |
| Anthropic Budget | 可开关 | 旧模型使用 thinking budget 映射 |
| MiniMax M3 | 可开关 | 没有通用 effort；高速能力通过提供商公布的模型 ID 选择 |
| MiniMax M2.x | 固定开启 | 没有通用 effort；highspeed 属于模型/线路选择 |
| MiMo | 可开关 | 没有通用 effort |
| Cursor Agent SDK | 无独立开关 | reasoning 取决于 SDK 事件与所选模型 |
| 通用 OpenAI 兼容 | 默认不发送 Thinking 参数 | 网关能力不确定，必须以真实测试结果为准；只有明确支持 `reasoning_effort` 的网关才选择“通用 Reasoning 兼容” |

PA_Agent 不伪造一个所有厂商都支持的“速度”参数。能可靠控制时使用原生推理强度；Flash / Highspeed 等速度差异通过模型 ID 选择。

连接测试到达截止时间后会立即把控制权还给界面，并禁止同时启动第二个底层探测。若供应商 SDK 无视超时且一直不退出，界面会提示“前一次底层探测仍在终止”；持续出现时请重启 PA_Agent。


### 通用设置


| 设置项         | 说明                | 默认值           |
| ----------- | ----------------- | ------------- |
| 用于分析的 K 线数量 | 提交分析时发送的已收盘 K 线根数 | 100           |
| 刷新间隔        | MT5 数据刷新频率        | 1000 ms       |
| 上下文警告阈值     | Token 用量警告线       | 80%           |
| 增量分析最大新增K线  | 自动增量的阈值           | 10 根（0 = 关闭）  |
| 交易倾向        | 保守/均衡/激进/极度激进     | 均衡            |
| 上次品种 / 上次周期 | 记忆上次使用的品种和周期      | XAUUSDm / 15m |
| 决策树播放       | 分析完成后自动播放路径       | 开启            |
| 播放时长        | 路径动画持续时间          | 50 秒          |
| 决策树可视化默认缩放  | 相对整图适配的缩放百分比      | 500%          |


---

## 14. 交易倾向（Decision Stance）

交易倾向影响**阶段二**的交易决策倾向，不影响阶段一的市场诊断。

### 四个倾向档位


| 档位       | 说明           | 典型行为                                         |
| -------- | ------------ | -------------------------------------------- |
| **保守**   | 默认裁定标准，与旧版一致 | 优先清晰信号；边际情况倾向不下单；§10.3 须 RR≥1.0 且交易者方程通过 |
| **均衡**   | 比保守更愿意交易     | 允许次优但可执行的二类 setup；边际方程通过时可下单；RR≥1.0（程序在 RR>1.5 时自动扩 stop） |
| **激进**   | 比均衡更愿意交易     | 接受更早更不完美的入场；RR≥1.0 且方程通过即可评估下单 |
| **极度激进** | 强制选方向和进场方式   | **必须给出具体做多/做空方案**；不可因犹豫输出"不下单"（§14 硬性禁止除外）   |


### 设置方式

1. 设置对话框 → 通用 → 交易倾向
2. 或直接修改 `config/settings.json` 中的 `general.decision_stance`

---

## 15. 策略文件路由机制

阶段一诊断完成后，系统根据 `cycle_position` + `direction` + `detected_patterns` 自动路由加载对应策略文件到阶段二 Prompt。

### 路由规则


| 市场状态           | 方向  | 加载的策略文件                                     |
| -------------- | --- | ------------------------------------------- |
| 微型/窄/正常/宽通道    | 上涨  | 上涨通道分析识别.txt + 上涨通道交易策略.txt + 窄通道与宽通道策略.txt |
| 微型/窄/正常/宽通道    | 下跌  | 下跌通道分析识别.txt + 下跌通道交易策略.txt + 窄通道与宽通道策略.txt |
| 尖峰             | 上涨  | 极速上涨分析识别.txt + 极速上涨交易策略.txt                 |
| 尖峰             | 下跌  | 极速下跌分析识别.txt + 极速下跌交易策略.txt                 |
| 尖峰（过渡中）        | —   | 按通道文件加载                                     |
| 尖峰（结束中）        | —   | 尖峰文件 + 通道文件                                 |
| 交易区间 / 趋势型交易区间 | —   | 震荡区间分析识别.txt + 震荡区间交易策略.txt                 |
| 极端交易区间 / 未知    | —   | **不加载策略文件（不下单）**                            |


### 叠加文件（基于 detected_patterns）


| 检测到的形态                            | 叠加文件                   |
| --------------------------------- | ---------------------- |
| 楔形 (wedge)                        | 文件14-楔形形态分析交易.txt      |
| 反转尝试 / H2 / L2 / MTR / final_flag | 文件15-二次入场机会.txt        |
| H1 / H2 / L1 / L2                 | 文件19-H1H2-L1L2计数.txt   |
| 突破失败 / 突破测试                       | 文件18-突破失败与突破测试.txt     |
| AlwaysIn / 20GB / Gap Bar         | 文件20-AlwaysIn与20GB.txt |
| 铁丝网 / 重叠 / 中间区域                   | 文件21-铁丝网与无交易环境.txt     |
| 失败信号 / 磁力位 / 被困交易者                | 文件22-信号失败后的磁力位.txt     |


### 始终加载的基础文件

- `提示词大纲_人设与思维方式.txt`
- `市场诊断框架.txt`
- `文件16-K线信号识别.txt`
- `文件17-止损和止盈与仓位管理.txt`
- `逐棒分析检查单.txt`
- `二元决策.txt`（阶段二）

---

## 16. 数据源与K线刷新

### MT5 数据源

- 程序启动时自动连接本地 MetaTrader 5 终端
- 支持 MT5 所有时间周期：1m / 2m / 3m / 5m / 10m / 15m / 30m / 1h / 2h / 3h / 4h / 6h / 8h / 12h / 1d / 1w / 1M
- 品种名称需与 MT5「市场报价」中完全一致（含后缀如 `m`）

### TradingView 数据源

- 通过 tvDatafeed 拉取（可无账号匿名，数据可能受限）
- 现货黄金等需配置 **交易所**（如 OANDA + XAUUSD）

### Longbridge 数据源

- 通过官方 `longbridge` SDK 的 `QuoteContext` 拉取常规交易时段、原始不复权 K 线；不会创建 `TradeContext`，因此不具备查询持仓或下单能力
- 凭据从进程环境或项目父目录的 `Quant\env` 读取：`LONGBRIDGE_APP_KEY`、`LONGBRIDGE_APP_SECRET`、`LONGBRIDGE_ACCESS_TOKEN`
- 选中 Longbridge 后，主界面会显示“只读行情”和 Token 到期状态；若 Token 可解析出 `exp`，同时显示到期日期，7 天内到期会预警，已到期会在连接前阻断
- 该日期来自本地 Token 内容，不等于服务端实时验签或撤销状态；“到期日未知”时仍会在真实连接阶段由 Longbridge 服务端校验
- 品种必须使用 `ticker.region`：如 `AAPL.US`、`700.HK`、`600519.SH`、`000001.SZ`
- 切换或更换品种时会先向 Longbridge 验证券代码；代码不存在或无行情权限时不会断开原数据源
- Longbridge 断线后再次点击“获取数据”会使用同一数据源配置重建只读连接
- `XAUUSD` 不是 Longbridge 证券代码；`GLD.US` 是黄金 ETF 代理，并非现货黄金
- 支持 1m / 2m / 3m / 5m / 10m / 15m / 20m / 30m / 45m / 1h / 2h / 3h / 4h / 1d / 1w；用于分析的 K 线数量最多设为 945，界面会限制并在切源前检查
- 日线按中、美、港市场的常规交易时段判断收盘；周线结合 Longbridge 交易日历判断本周最后交易日，周末不会继续误判为形成中
- 盘中周期会按午休等常规时段分段截断；特殊半日市缺少可靠的提前收盘时刻字段，可能到常规收盘时间才标记闭合

PA_Agent 当前不会登录 Longbridge 网页替你读取或重置 Legacy Access Token。Token 到期前，请在 Longbridge 开放平台生成或刷新 Token，手动替换 `Quant\env` 中的 `LONGBRIDGE_ACCESS_TOKEN`，然后在 PA_Agent 中重新连接数据源。SDK 虽提供刷新能力，但自动刷新还需要安全持久化新 Token、重建连接并处理失败恢复；OAuth 2 则需要单独应用和首次浏览器授权，本次只读接入均未启用。

### 刷新循环（RefreshLoop）

- 默认每 1 秒从当前数据源拉取最新 K 线；Longbridge 自动限制为至少 2.5 秒
- 新数据到达后自动更新图表
- 刷新间隔可在设置中调整（100–10000 ms）

### 图表冻结与恢复

- 提交分析时，图表自动冻结在分析瞬间的K线快照
- 防止分析期间K线更新导致 AI 看到的数据与图表不一致
- 点击「图表实时更新」可手动恢复

---

## 17. 经验库系统

### 目录结构

```
experience/
├── broad_channel/       # 宽通道
│   ├── success_cases/   # 成功案例
│   └── failure_cases/   # 失败案例
├── extreme_tr/          # 极端交易区间
├── micro_channel/       # 微型通道
├── normal_channel/      # 正常通道
├── spike/               # 尖峰
├── tight_channel/       # 窄通道
├── trading_range/       # 交易区间
├── trending_tr/         # 趋势型交易区间
└── unknown/             # 未知/其他
```

### 工作原理

1. 每次分析完成后，系统根据诊断结果的 `cycle_position` 在对应目录中加载经验案例
2. 成功案例和失败案例都会被加载
3. 经验案例作为 Prompt 的一部分发送给 AI，帮助其参考历史类似行情
4. 在「调试」标签中可查看本次加载的经验条目数

---

## 18. 记录持久化

### 分析记录

每次分析完成后自动保存到 `records/pending/` 目录：

- **主文件**：`YYYY-MM-DD_HH-mm-SS_品种_周期.json`
  - 包含完整的两阶段数据：K线、Prompt、AI 响应、诊断结果、决策结果、Token 用量
- **对话文件**：`YYYY-MM-DD_HH-mm-SS_品种_周期.followups.jsonl`
  - 自由对话的逐条记录

### 记录内容

每条分析记录包含：


| 字段                  | 说明                               |
| ------------------- | -------------------------------- |
| meta                | 时间戳、品种、周期、K线数、AI 配置快照、交易倾向       |
| kline_data          | 发送给 AI 的K线数据                     |
| htf_text            | 高级时间框架文本                         |
| stage1_messages     | 阶段一发送的完整消息（system + user）        |
| stage1_response     | 阶段一 AI 原始响应（含 reasoning_content） |
| stage1_diagnosis    | 阶段一解析后的诊断 JSON                   |
| stage2_messages     | 阶段二发送的完整消息                       |
| stage2_response     | 阶段二 AI 原始响应                      |
| stage2_decision     | 阶段二解析后的决策 JSON                   |
| strategy_files_used | 使用的策略文件列表                        |
| experience_loaded   | 加载的经验案例列表                        |
| exception           | 异常信息（如有）                         |
| usage_total         | Token 用量汇总                       |


---

## 19. 安全与密钥管理

### API Key 存储

- 输入框默认以圆点遮罩，离开输入框后会自动重新隐藏；这只是界面保护，不是磁盘加密
- 当前实现会把各模型档案的 API Key 明文写入本机 `config/settings.json`
- `config/settings.json` 默认被 Git 忽略，但不要发送、截图或上传该文件；需要更高安全级别时，应先改为系统密钥库再保存凭据
- `api_key_encrypted` 仅是兼容保留字段，当前不提供有效的 DPAPI 加密存储

### 日志脱敏

- 所有日志输出中 API Key 自动替换为 `*`*** 掩码
- 调试面板中 API Key 同样脱敏显示

### Git 安全

- `settings.json` 在 `.gitignore` 中排除
- 提供 `tools/setup_git_secrets.ps1` 脚本，设置 pre-commit hook 拦截敏感文件

---

## 20. 常见问题与故障排除

### 启动问题


| 问题                                                | 解决方案                                        |
| ------------------------------------------------- | ------------------------------------------- |
| `ModuleNotFoundError: No module named 'pa_agent'` | 在项目根目录执行 `pip install -e ".[dev]"`          |
| `No module named 'PyQt6'`                         | 执行 `pip install PyQt6`                      |
| `DLL load failed`                                 | 安装 Visual C++ 运行库                           |
| MT5 未连接                                           | 确保 MT5 终端已打开并登录                             |


### 分析问题


| 问题                | 原因与解决方案                                                           |
| ----------------- | ----------------------------------------------------------------- |
| 品种红色警告            | 品种名与 MT5 不一致，检查后缀（如 `XAUUSDm`）                                    |
| "数据不足"            | 等待缓冲区填充（几秒后重试）                                                    |
| 阶段一/阶段二 JSON 验证失败 | 按 `ValidationSettings` 的开关与次数自动重试；重试关闭或用尽后弹出调试对话框并切到「原始」页，可复制完整 exception / 规则列表排查 |
| 阶段二感觉像「又跑了一遍」     | 程序内仅单次阶段二请求；日志里 `retrying without stream_options` 是网络层兼容重试，不是业务重试 |
| JSON 截断           | 思考区 token 用尽导致正文未写完；可缩短 prompt 或调整 Reasoning Effort               |
| 网络超时              | 检查网络连接和 API 服务可用性                                                 |


### 配置问题


| 问题                 | 解决方案                  |
| ------------------ | --------------------- |
| `settings.json` 损坏 | 删除该文件，程序自动重建默认配置      |
| API Key 无效         | 打开“AI 模型设置”重新输入，点击“测试并保存”；查看“连接 / 认证”结果 |
| 模型或 Base URL 错误    | “参数接受”或连接测试会失败；修正后必须重新测试，旧测试状态自动失效 |


### 性能问题


| 问题         | 解决方案                               |
| ---------- | ---------------------------------- |
| 图表卡顿       | 减少 K线数量；降低刷新间隔                     |
| Token 用量过高 | 减少 K线数量；使用增量分析；降低 Reasoning Effort |
| 分析耗时过长     | 使用更快模型；降低思考深度                      |


---

## 快捷参考卡

```
┌─────────────────────────────────────────────────────┐
│              PA Agent 核心操作速查                     │
├─────────────────────────────────────────────────────┤
│ python run.py              启动程序                   │
│ [提交分析]                 完整两阶段 AI 分析          │
│ [增量分析]                 基于上次记录做增量分析       │
│ ☑等待收盘 → [提交分析]     等K线收盘后自动分析         │
│ [演示模式]                 回放历史记录（不消耗API）    │
│ 菜单→AI 模型设置           管理、测试与切换模型档案      │
│ 菜单→其他通用设置          配置 K线数量 / 倾向 / 刷新等  │
│ 「实时」标签               查看流式分析输出 + 自由对话  │
│ 「决策树可视化」标签         查看决策路径流程图          │
│ 「决策」标签               查看结构化交易决策           │
│ 「原始」标签               调试 AI 请求/响应原始数据    │
└─────────────────────────────────────────────────────┘
```

