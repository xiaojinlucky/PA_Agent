# PA_Agent Context

## 当前状态（2026-07-26）

> 静态代码、规则和文档事实可直接引用；进程、账户、服务、Live/Demo 配置、仓位和券商状态若没有本轮明确证据，均按历史快照处理，必须在本机重新核验。

- 2026-07-27 08:40（北京时间）**WO-F Claim Validation 反幻觉层落地 + CI 时区修复（未提交）**：①截断修复默认关闭（`disable_truncation_repair` 默认 True），宽松修复只出 `gate_result=unknown`，AUTO 桩的 unknown 绝不再被洗成 proceed；②不下单+非空价位不再静默清空、突破单缺挂单依据不再降级限价单——都交给 schema 拒绝（category c）并走带反馈重试；③next_bar argmax 平局改确定性规范序；④新增 `validate_price_grounding`：entry/stop 必须落在已收盘 OHLC 包络 ±max(ATR14,3×tick)（TP 允许投射区间外，由 RR/几何锚定）。**tests/property 6 个历史失败清零（54/54）**，集成既有失败 8→7，全量 unit 保持历史 18 项零新增。CI 唯一红项（`_local_time` 时区）按产品语义显式固定 Asia/Shanghai，UTC 环境模拟证明转绿。新校验随下次 Campaign 空仓安全重启生效。总控工单进度账本同步在 `docs/WORKORDER_MASTER_20260727.md`。
- 2026-07-27 07:35（北京时间）**通宵无人值守运行 + 发布 + 代码重载**：Campaign `ab245e48` 于 23:10–07:25 完成 **50 根自然 10m K 线、0 失败**；北京 05:31 自然信号真实提交 OKX Demo 限价单（execution `ed7711be`，submit 命令 succeeded），270 秒未成交按规则撤销，未切市价——完整自然交易生命周期实证。四个模块化 commit（`372cd26`/`aa1c91d`/`a5d2e19`/`b539cb5`）经逐 commit gitleaks 零泄漏后已推送 `origin/main`（用户确认在先）。随后按硬门（kill=0、活动 exec=0、命令 0、租约已过期、两库 ok、券商侧空仓空挂单）停旧进程并以 restart 语义重启 Campaign（07:33，加载含市场规则块/交易所时区的新提示词代码），首根自然 K 线观察中。**自包含总控工单**（供 GPT 等接手）：`docs/WORKORDER_MASTER_20260727.md`。P2 观察项：租约 `4cf57283` 在其 execution 终态后仍被 Controller 滚动续期约 2 小时（不影响运行，随进程消亡；已记 WO-G）。
- 2026-07-27 00:00（北京时间）**固定代理硬阻断已解决，交易红线恢复**：22:41 经候选 3 轮全账单 + 3×90 秒空闲耐久闸门后，固定代理从好猫 US1（AnyTLS，空闲后持续 SSLEOFError）原子切换到桔子云 OKX 日本 1（VMess，出口 216.38.169.153 已在 OKX 白名单，sing-box PID 8588），切换后 SSLEOFError 归零。探针脚本已固化空闲耐久闸门（`--idle-seconds/--idle-cycles`，激活强制 ≥1 周期）并把 sing-box 模板默认改为运行目录 config.json（v2rayN 主核心切 xray 后其 binConfigs 不再是 sing-box 格式）。Worker 连续 ≥9 个自然完整扫描（kill_activated_at 冻结、心跳/对账实时）后，23:09 专用恢复命令 `0e525d57` 成功：`kill_active=0`，高水位保持 `78303.57015174496` 未重锚，账户身份不变。23:10 Campaign `ab245e48-218a-4fac-8445-10c9893e868e` 以 restart 语义恢复（冻结 `20000/10%/20x/risk_budget`，继承 last_completed_bar_ms 不重复旧 K 线），3 根自然 10m K 线全部完成：23:00 根真实做多信号被杠杆容量硬闸门阻断（`user_max_leverage_capacity_insufficient`，未截断未强制）、23:10 根自然 `no_order`、23:20 根同为杠杆阻断；0 失败，Campaign 保持 active 常驻。独立只读交叉审计无 CRITICAL（高水位/身份逐字符一致、券商侧零持仓零挂单、kill 解除仅那一条授权命令）。
- 2026-07-27 00:00（北京时间）**P1 多市场看盘后端已落地**（前端市场切换 UI 按 GUI 强制设计流程另行推进）：① 共享合同层 `D:\Desktop\Quant\shared\market_contracts`（规范符号/K 线合同/三市场日历/QuoteAdapter+TradeAdapter 协议，39 测试全过，已 editable 安装进本 venv；PA_Agent 仓库自身不 import 它以保持公共仓库 CI 自包含）；② `LongbridgeSource` 正式化：`history_candlesticks_by_offset` 分页（上限 3000 分析根、5000 硬顶）、10 次/秒频控、15 秒 SDK 卡顿保护（#380）、交易时段内缺根校验（#890，按 exchange_calendars 判定午休/休市合法间隔）、新增 `latest_snapshot_for_timeframe`（长桥品种自此有 1h/4h 高周期背景）；③ `pa_agent/data/market_calendar.py`（XNYS/XHKG/XSHG 会话状态，半日市识别）；④ 市场规则块 `prompt_engineering/市场规则_{A股,港股,美股,加密}.txt` + `pa_agent/ai/market_rules.py` 按符号路由注入 Stage1/Stage2 用户回合（不进 system，保护 DeepSeek 前缀缓存；未归类符号明确不注入）；⑤ K 线表时间列改按交易所时区渲染并标注（美东/香港/北京/UTC）。`pyproject` 新增 `exchange_calendars>=4.5`。**运行中 Campaign 仍是 23:10 加载的旧提示词代码，对抗审查通过后需一次空仓安全重启加载**。
- 2026-07-27 00:00（北京时间）**硬阻塞（仅用户可解）**：长桥 access token 被服务端拒绝（`401004 token invalid`，本地 exp 预检通过，说明是服务端作废——与密钥轮换/泄漏处置相关）。P1 三标的（AAPL.US/700.HK/600519.SH）真实两阶段验收脚本 `scratch/p1_multimarket_acceptance.py` 已就绪，等用户在长桥后台重签 token 并更新 `D:\Desktop\Quant\env` 后即可运行。
- 2026-07-26 16:00（北京时间）当前 Goal 已按用户要求整理为 Claude 交接包：`docs/CLAUDE_HANDOFF_20260726.md`（其 §8 固定代理阻断已按上述解决，文件顶部有解决横幅）。前端主工作台、结构化读模型、Controller→Worker 唯一写链联通、四状态多尺寸离屏图、空仓账户刷新冷却、固定代理双闸门、ACL 和安全因果日志已经落到未提交工作区；当时完整单测为 `1618` 通过、`18` 个历史失败。
- 2026-07-26（设计规则，不是运行态证据）：PA_Agent 已纳入 Quant 统一 `$frontend-design` 和 `$design-taste-frontend` 流程。任何 GUI 新页面或重大改版必须先冻结真实功能/字段/状态/交互合同，再完成 Product Design 三方向、用户审美确认、网页版 ChatGPT PRD、脱敏材料与 PRD 的 Stitch 设计、至少三轮图片精修和真实 Taste 视觉审计，之后才能转换为 PyQt6/QSS 并接通真实读写链。所有副标题、eyebrow/tagline、标题下解释、灰色辅助小字、括号注释、营销口号、重复说明和小号页脚一律禁止；必需的标签、单位、时间、状态、错误及风险阻断原因必须以正常可读字号与清晰对比度显示。GSAP 不进入 PA_Agent，动效只允许 Qt 原生实现。
- 2026-07-25 13:53（北京时间）OKX 已切到 PA Agent 独占的本机固定代理 `127.0.0.1:10981`，固定使用橘子云 `V1-137|美国|x2.0`，与用户日常 v2rayN `10808` 节点完全分离；出口 IP 为 `157.254.38.138`，`NO_PROXY=*` 下 Demo 私有只读预检通过。最终 Worker PID `49056` 已加载显式不可绕过代理代码，实测与 `10981` 保持连接；固定节点在 13:46 再次短暂超时后，Campaign 于新一根 13:50 K 线只创建一次专用恢复命令 `04f76bdd-1c42-4cc5-af51-0e4dc7ac69f4`，先完成新的完整风险读取，再成功解除 `risk_runtime_BrokerTransportError`，风险高水位保持 `78303.57015174496`、未重锚。Campaign `22e41dc3-a535-43d0-b0aa-bfb873ce548f` 冻结 `20000 USDT / 10% / 20x` 并保持 `active`；恢复后没有跳过同一根 K 线，13:52 已完成分析，累计成功 6、失败 1，最近自然结果 `blocked:no_order`，没有强制下单。最终风险停止为 0、活动 execution 为 0、未解决券商写命令为 0、`NEW_RISK` 租约为 0。固定代理由当前用户的隐藏登录启动项守护；主 v2rayN 可以自由切换，但固定节点仍偶有抖动，失效时 PA 必须闭锁新增风险并在下一根新 K 线只读复核，不得自动跟随主节点。电脑重启后仍依赖用户登录启动固定代理，当前不能宣称跨重启无人值守。
- 2026-07-25 09:09（北京时间）视觉与安全双 Agent 对抗审查提出的 5 项问题已全部修正：快照读取失败立即废弃旧账户/执行数据并闭锁按钮；陈旧 execution 在决策卡和表格明确标为“本地账本记录，待券商核对”；`realized_pnl` 明确标注为“券商已实现盈亏（未扣费用）”；Worker 原始失败码只进入技术详情；固定张数的最坏损失或最低保证金超过有效风险资本时编辑态立即标红并说明整笔阻断。最终工作台定向 `22` 通过、`0` 失败，受影响执行/风险/Campaign/设置/GUI 范围 `516` 通过、`0` 失败；Ruff、`compileall`、`git diff --check` 通过，最新 1440×900 与 1920×1080 离屏图已人工复核；原审查者回看修正后全部 `PASS`，无新增 P0/P1。
- 2026-07-25 08:57（北京时间）网页版 GPT 的视觉审核已在本地修正为 `docs/prd/04_PA_Agent_Stitch视觉重构PRD.md`，并通过用户本机 Chrome 成功提交 Google Stitch；Stitch 已生成健康空仓、配置编辑、持仓保护和风险阻断四种桌面状态，但仍错误增加了重复左导航，本地实现明确不采纳。`trading_workbench.py` 当前区分“当前运行参数 / 下次启动参数”，增加未保存横幅、取消编辑、风险账户专用重新检查、真实保护数量结论和折叠技术详情；主视图不再显示手动执行入口，撤单/主动离场只在真实可达状态出现，账户不可信时主动离场隐藏。`1440×900`、`1920×1080` 和固定张数编辑态离屏图已人工复核；受影响执行、风险、Campaign、设置和 GUI 范围 `510` 通过、`0` 失败，Ruff、`compileall`、`git diff --check` 通过。
- 2026-07-25 08:55（北京时间）当前公网 IP 已变为 `188.253.121.195`，OKX Demo 私有只读接口返回 `50110`，明确拒绝未加入白名单的 IP。Worker Windows 服务仍为 Running、心跳持续，但状态为 `needs_attention / RuntimeError`，最后成功全局对账停在 06:43:50；Campaign 进程已不存在，状态文件 05:00:20 变为 `needs_attention`，12 成功/0 失败，最近 `blocked:risk:transient_read_unavailable`。执行账本仍记录自然空单剩余 `56862` 张及 OCO `3772100521721503744` 为 live，但这是陈旧账本；最后精确券商证据停在 06:43:44。公开行情之后已越过旧止损触发价 `4064.8`，服务器端 OCO 可能已触发，但没有私有证据，禁止写成已平仓。当前无 `NEW_RISK` 租约；白名单恢复前不得重启 Campaign、恢复风险或写券商。
- 2026-07-25 03:35（北京时间）最终受影响范围回归 `319` 通过、`0` 失败；完整单元测试共 `1550` 项，`1532` 通过、`18` 失败，18 项与本阶段前相同，仍是既有一致性/连续性、旧预测面板、音效、价格跳动、OpenClaw 和宽松枚举映射。`compileall pa_agent tests`、`git diff --check` 和本阶段文件 Ruff `E/F/I/UP/B/C4`（排除既有行长）通过。最终只读对抗复审为 `P0/P1/P2 全 PASS`、无残余阻断；仓库全规则 Ruff 历史债务仍使 PRD 第 16 条按严格口径不通过。
- 2026-07-25 03:26（北京时间）最终对抗审查发现并修复固定张数主链遗漏：定仓模式、固定张数、实际风险金额、名义价值和单张最坏损失现在随不可变 ExecutionPlan 进入 Worker；OKX 写前按同一模式复算并逐项比较，固定模式不再使用隐藏的风险百分比，配置指纹也纳入定仓模式。真实 `Controller → Worker → ExecutionService → OkxAdapter → OKX POST 替身` 全链证明固定 2 张只产生一次 2 张入场 POST；单张损失变化但数量仍相同时会在预检/POST 前阻断。杠杆授权在固定模式只绑定资金上限、模式、固定张数和最大杠杆，不再错误比较隐藏风险比例。
- 2026-07-25 03:26（北京时间）Campaign 临时风险恢复新增耐久的 `risk_recovery_bar_ms / risk_recovery_command_id`：先落本根尝试标记、再创建唯一 Worker 命令、超时或重启后只等待同一命令；若崩溃发生在命令入队与 ID 落盘之间则跳过本根，绝不重复入队。重启恢复同根 execution 时，`ERROR`、`UNKNOWN` 或 `needs_attention=true` 一律停下人工核对，只有活动状态或明确安全终态可以继续监控/继承。当前运行中的 Campaign 在这些最终字段写盘前已启动；由于仍有真实受保护仓位，不做热重启，磁盘修复将在下次空仓安全重启后加载。
- 2026-07-25 03:26（北京时间）完整 `MainWindow` 的 `1440×900` 离屏图 `scratch/main_window_trading_1440x900.png` 已人工检查：一级页签、菜单和状态栏均在，普通视图只显示 OKX Demo 记录，陈旧账户快照不再显示旧资金/持仓，未知风险不标绿，UTC 运行时间转换为北京时间，Worker/Campaign/event 原始状态改成人话。固定张数页新增按最近有效入场/止损的只读预估；保证金明确标为“按最大杠杆估算的最低保证金”，证据不足时显示等待而不猜值。
- 2026-07-25 03:24（北京时间）自然空单 `aa90ddfe-08e7-5fa8-a91f-49da9170dec8` 已完成第一档止盈：原始 `113724` 张空单实现 `56862` 张，剩余真实净仓位 `-56862`；剩余原生 OCO `3772100521721503744` 为 `reduceOnly`、数量恰好 `56862`，止盈 `4050.2`、止损 `4064.8`，普通挂单及其余算法单均为 0。Campaign 新 K 线选择同向持有、没有重复加仓；风险停止为 0，03:10 的专用恢复命令成功且高水位未重锚。执行账本的 `+77.4391` 是价格差口径，OKX 仓位字段的净已实现盈亏为 `-164.77835052`，费用/资金费口径尚需单独核对，当前不能把前者写成净利润。
- 2026-07-25 02:57（北京时间）“实盘交易”已从拥挤配置弹窗重构为主窗口一级工作台：顶部只显示 OKX Demo、Campaign、Worker、风险和更新时间；中央显示最新 PA 决策、订单/持仓生命周期和最近事件；右栏区分账户资金、当前生效配置与下次启动配置。风险预算模式由用户设置资金上限/风险比例/最大杠杆并唯一计算张数；固定张数模式由用户设置张数/最大杠杆并反算最坏损失，禁止静默减量。`1440×900`、`1920×1080` 和固定张数模式离屏截图均已人工检查，无重叠或横向挤压；用户仍需自行关闭并重新打开 `D:\Desktop\PA_Agent.lnk` 才能加载磁盘新 GUI，Agent 不操作桌面和鼠标。
- 2026-07-25 03:05（北京时间）新版 Campaign 第二根自然 10m K 线产生并真实执行空单 `aa90ddfe-08e7-5fa8-a91f-49da9170dec8`：授权有效资本 `20000 USDT`、最坏损失预算 `2000 USDT`、数量 `113724` 张；OKX Demo 实际均价 `4059.4012750167071155`，真实净持仓 `-113724`，杠杆 `20x`。两张原生 OCO `3772100057026174976 / 3772100521721503744` 各保护 `56862` 张，止损均为 `4064.8`，止盈分别为 `4057.5 / 4050.2`，保护总量与持仓完全相等；普通挂单 0、其余七类算法单 0、execution=`open`、`needs_attention=false`。这是自然 PA 信号，不是 canary 或强制成交；Campaign 和 Worker 保持运行并继续只读对账。
- 2026-07-25 02:57（北京时间）临时 OKX 私有读取故障已脚本化为“每根新的 10m K 线最多复核一次”：只允许 `BrokerApiError/BrokerTransportError/IncompleteRead/50004` 走专用 Worker 命令，命令必须先取得新的完整账户、身份和账单链证据；成功后不重锚高水位，失败则该根 K 线直接记为不下单。回撤、身份变化和账本完整性停止绝不自动解除。专用命令 `4cc7e044-5f99-4241-a9f8-928d7b8e4870` 成功，高水位保持 `78303.57015174496`；新版 Campaign `73451990-ce0f-490d-be94-bc3522d86699` 进程树 `52720 → 26936` 已加载并完成一根自然 10m K 线，PA 结果为 `blocked:no_order`，Campaign 继续 `active`。
- 2026-07-25 02:57（北京时间）重载前的旧 Campaign 在风险停止重新出现的竞态中创建 execution `6cb04c25-9d30-597c-be8a-1900850d5c62`；Worker 的提交前风险闸门把它确定阻断为 `BLOCKED`，命令状态 `FAILED`、券商订单号为空，真实回查仓位/普通挂单/八类算法单均为 0。新版 Campaign 现在把这种确定性 `BLOCKED` 当作本根 K 线完成并继续运行，不再错误退出为 `needs_attention`；Campaign 重启也会从耐久分析记录核对并继承已经进入终态的 inflight K 线，防止重复分析或重复交易。
- 2026-07-24 23:28（北京时间）用户把当前公网 IP `154.40.63.187` 加入 OKX API 白名单后，私有只读接口恢复。硬门确认空仓、普通挂单 0、八类算法单 0、活动 execution 0、待处理命令 0；专用命令 `3cc99df4-8463-47da-8984-a587b2fa5490` 先完成完整风险刷新，再解除 `risk_runtime_BrokerTransportError`，高水位保持 `78303.57015174496`、没有重锚。Campaign 以当前源码恢复为 PID `51428 → 22204`，沿用 ID `677c70df-aa5d-442a-9809-1896b7dc2994` 和最后完成 K 线，不重复旧轮次；状态已真实冻结 `20000 USDT / 10% / 20x`。
- 2026-07-24 23:28（北京时间）恢复后的 23:10 已收盘 K 线产生真实 PA 多单 execution `25ca0e71-0cfd-573a-94ef-b04be6edbaa1`：固定风险预算 `2000 USDT`、数量 `82630`、入场 `4070.6`、止损 `4062.0`、止盈 `4085.5 / 4104.0`。唯一 Worker 成功提交 OKX Demo 限价单 `3771656369780903938`；等待 270 秒成交 0 后正常撤销，没有切市价。最终 execution=`canceled`、remaining=0、attention=false、error 空；23:27:32 私有回查确认仓位 0、普通挂单 0、八类算法单全 0。Campaign `active`，累计 9 成功/0 失败，最近 `execution:canceled`；Worker PID `55264` 健康，kill=0，未解决 uncertain=0。停机期间 22:00–23:00 的中间 K 线没有分析，不能冒充已补跑。
- 2026-07-24 22:38（北京时间）实时核验发现红线 1 当前已中断：Campaign 最后在 22:01:33 确认正常，已完成 8 轮、失败 0、最近 `blocked:no_order`、无进行中轮次；22:16 已找不到进程，因此退出发生在 22:01:33–22:16 之间。状态文件没有退出错误，不能把 22:09 后的 IP 故障直接说成已证实的退出原因。Worker 服务和 PID `55264` 仍存活，但 22:09 后 OKX 私有接口持续返回 `50110`，明确指出当前公网 IP `154.40.63.187` 不在 API Key 白名单；Worker 因此为 `needs_attention / BrokerApiError`，风险运行态按安全规则保持 `kill_active=true / risk_runtime_BrokerTransportError`，没有 `NEW_RISK` 租约。最后一份精确的券商空现场证据停在 21:32:53（仓位 0、普通挂单 0、八类算法单 0）；当前 IP 无权取得更新证据，所以不能声称此刻仍空仓，也不能安全恢复 Campaign。控制库与执行库 `integrity_check=ok`，本地没有活动 execution；需用户先切回已加入白名单的 VPN 节点或更新白名单，再做私有只读硬门、专用临时故障恢复和 Campaign 最短重启。
- 2026-07-24 22:15（北京时间）桌面入口已核验为 `D:\Desktop\PA_Agent.lnk → D:\Desktop\Quant\PA_Agent\.venv\Scripts\pa-agent.exe`，不是另一套仓库。此前用户看到的是 15:23 启动的旧 GUI 与 20:56 重启的新 Worker/Campaign 同时存在，旧 GUI 不会热加载磁盘代码。磁盘最终源码已具备仓库路径、GUI 代码加载时间、10 分钟 Campaign 状态和固定 `20000 USDT / 10% / 20x` 显示，正式 GUI 行情配置也已对齐为 `OKX / XAU-USDT-SWAP / 10m`。最后一次 21:36 启动的窗口早于 `main_window.py/read_models.py` 的 21:50–21:51 最终修改，且 22:15 只读进程检查已确认桌面 GUI 当前未运行；等待用户自行从正式快捷方式启动并截图验收。用户明确禁止 Agent 继续操控桌面和鼠标，后续界面核验只能由用户截图。
- 2026-07-24 22:34（北京时间）对抗复审提出的四项安全缺口已在磁盘源码修复：测试在模块收集前移除真实券商环境变量、封死共享凭据路径并覆盖子进程；Campaign 状态耐久保存启动时冻结的 `风险资本上限 / 风险比例 / 最大杠杆`，GUI 同时显示“GUI 配置”和“实际冻结参数”，不一致或未知时标红；所有 Campaign 状态、进度和最近结果都统一受 15 分钟新鲜度约束；GUI 保存配置改为先成功落盘，再更新共享内存和服务。定向回归 `188` 通过、`0` 失败；完整单元测试 `1493` 通过、`18` 失败，18 项均为同一历史范围，没有新增失败；Ruff、`compileall` 和 `git diff --check` 均通过。当前后台 Campaign 仍是修改前启动的旧进程，状态文件还没有新冻结字段，必须在空仓、空挂单、无活动执行/命令且 Worker 健康的硬门下做最短安全重启，才能让 GUI 显示真实运行参数；在此之前不得写成当前进程已加载。
- 2026-07-24 22:34（北京时间）用户看到的 `pytest-qt-qapp / test` 小窗口已确认为 Qt 自动测试窗口，不是生产交易窗口。测试入口现在不只强制 `QT_QPA_PLATFORM=offscreen`，还在测试模块导入前隔离正式设置、真实 OKX/Longbridge 环境变量和共享凭据文件，测试子进程继承同一隔离。用户批准删除的 117 个旧测试临时目录已经清除，正式数据库备份和日志未删除；本轮测试新生成的临时目录尚未清理，必须在完整列出后另行取得删除确认。
- 2026-07-24 21:50（北京时间）Campaign 已完成 6 根已收盘 10m K 线分析，6 成功、0 失败，最近结果均为自然 `blocked:no_order`，因此没有伪造交易。21:44 一次 OKX `URLError` 触发 `risk_runtime_BrokerTransportError` 停止；私有只读恢复后，专用命令 `5287e042-fe20-43bf-9614-b6f53ccbc6f9` 成功，只解除临时读取故障，风险高水位保持 `78303.57015174496`、未重锚。下一轮已进入分析，错误已清空；Worker PID `55264`、Campaign、空仓和空挂单状态继续由只读监控核验。
- 2026-07-24 21:00（北京时间）`WO-RUN-06-PROD-01` 已完成生产切换和真实 OKX Demo 闭环。最终 Worker 实际 PID `55264` 的启动时间晚于最终 `okx_adapter.py`，已加载固定 `20000 USDT / 10% / 20x`、统一风险闸门、v4 锁内迁移、独立临时故障恢复和保护撤销 `51603` 回查代码。生产库不可变 `v4_cutover_baseline` 明确记录 `historical_maximum_claimed=false`；风险高水位保持 `78303.57015174496`，两次专用恢复均未重锚。
- 2026-07-24 21:00（北京时间）真实受控 Demo execution `957e4d70-c6bd-509a-95d6-3cfcc48718d9` 使用 USDT 权益 `77702.87537394238`，固定资本取 `20000`，风险预算 `2000`，目标数量 `80116`；入场订单 `3771294118783832064` 全成，两张 OCO 各保护 `40058`。主动离场时 OKX 在撤单后对单笔详情返回 `51603`，旧代码一度卡在 `exit_pending`；修复后先查客户算法单、待处理单和三类历史，再在离场 POST 前紧贴执行点二次核验真实仓位、普通单和八类算法单。离场订单 `3771323397642997760` 全成，均价 `4061.4953754555893957`，execution 最终 `CLOSED/remaining=0/needs_attention=false`，记录已实现盈亏 `47.6991`。收口只读真值为仓位 `0`、普通挂单 `0`、八类算法单 `0`、活动 execution `0`、未解决 uncertain `0`。
- 2026-07-24 21:00（北京时间）最终执行/OKX/风险套件 `430` 通过、`0` 失败；OKX 适配器 `94` 通过、`0` 失败；完整单元测试共 `1497` 项，`1477` 通过、`20` 失败，20 项与工单前完全相同，没有新增失败。最终改动范围 Ruff `E4/E7/E9/F/I`、`compileall`、`git diff --check` 通过；对抗审查最终为 `PASS`。生产备份位于 `scratch/production-backups/execution-control-v4-before-fixed-cap-cutover-20260724-201323.sqlite3`，SHA-256 `2ad8a9f73e865ac66bdffe14f7187946b3af437aba6c6414fad9fdfa7da96e47`，原生 SQLite `integrity_check=ok`。
- 2026-07-24 21:00（北京时间）新的 Campaign `677c70df-aa5d-442a-9809-1896b7dc2994` 已恢复为 `active`，固定 `OKX Demo / XAU-USDT-SWAP / 10m / min_trade_confidence=20 / extreme_aggressive / entry+exit limit_with_slippage 0.50×ATR14`，继承 `last_completed_bar_ms=1784884200000`，没有重复旧 K 线。共享 Live 双硬门仍关闭；后续运行态必须继续由本机实时核验。
- 2026-07-24 20:03（北京时间）`WO-RUN-06-PROD-01` 当前代码与离线证据已收口，但尚未生产切换。现行固定策略为：用户风险资本上限 `20000 USDT`、单笔最坏止损风险 `10%`、最大杠杆 `20x`；有效资本取 `min(20000, 最新 USDT 权益)`。`totalEq` 只用于资金流、高水位和 50% 回撤；USDT 权益只用于固定资本定仓。SUBMIT 与 SET_LEVERAGE 共用写前风险刷新，风险拒绝明确记为 `FAILED`，只有已经发出但结果不明的券商写入才可记为 `UNCERTAIN`。普通 CLEAR 只允许解除 `drawdown_threshold_exceeded`；`BrokerApiError/BrokerTransportError/50004` 必须走独立的成功刷新复核命令，保留原高水位和账单游标。
- 2026-07-24 20:03（北京时间）当前代码已证明：旧 v1 库锁前打开字节和 schema 不变；v1/v2 只能由持有指定 Worker 单例锁的进程迁移至 v4；多条未解决命令、完整命令字段、处置证据与 SHA-256 摘要逐项保留；迁移不自动领取、重放或新建处置，迁移后 `PRAGMA integrity_check=ok`。首次可信风险基线另存为不可覆盖的 `v4_cutover_baseline`，不能冒充历史最高值。
- 2026-07-24 20:03（北京时间）最终受影响套件 `378` 通过、`0` 失败；完整单元测试 `1462` 通过、`20` 失败，失败数和节点均为本工单外的历史范围，没有新增失败；改动文件的 Ruff `E4/E7/E9/F/I`、`compileall`、`git diff --check` 共 `3` 通过、`0` 失败。全规则 Ruff 仍有历史 RUF/SIM/中文标点问题，不在本工单内批量改写。
- 2026-07-24 20:03（北京时间）生产现场仍处于维护状态：Campaign 已暂停，现役 Worker 尚未加载本轮最终代码，持久风险停止仍是 `risk_runtime_BrokerTransportError`。最近一次只读快照为空仓、普通挂单和八类算法单均为 0，活动 execution、命令、租约和未解决 uncertain 也均为 0；该快照必须在切换前再次实时核验。尚未执行本轮 SQLite 原生备份、Worker 重启、独立恢复命令、受控 Demo 闭环或 Campaign 恢复，文档不得写成“已生产完成”。
- 2026-07-24 16:57（北京时间）运行监控发现 OKX 账单端点再次返回 `50004`。风险运行态按硬门置为 `kill_active=true / risk_runtime_50004`；17:09 后账单扫描时间已持续前进，证明端点恢复，但持久停写没有自动清除。Worker 普通对账和 Campaign `active / 10m / 20 / extreme_aggressive` 均继续运行，活动 execution、待处理命令、`NEW_RISK` 租约、仓位和挂单均为 `0`。当前仍禁止新增风险；只有用户明确授权 CLEAR 后才可由唯一 Worker 重锚恢复，不能仅凭端点或普通账户接口恢复自行解除。
- 2026-07-24 对抗审查已修复监督数量完整性：自然与受控监督快照现在明确保存阶段二信号价、`0.50×ATR14` 后最终有效限价、止损、USDT 风险权益、10% 预算和技术数量；Worker 提交前仍刷新账户总权益与资金流水用于回撤闸门，但风险定仓统一使用 OKX 结算币 USDT 权益。提交前重算数量必须与监督冻结数量精确相等，任何增减都把旧计划阻断为 `risk_sizing_changed_after_supervision`。Demo-S 返回实际账本数量并同时保存监督数量；execution 创建后立即关联 Campaign。异常撤单后若竞态出现部分成交，会继续切换到一次主动离场；Campaign 重启继承最后完成 K 线，禁止重复分析或重复交易同一根。
- 2026-07-24 真实 OKX 容量复核确认当前 20× 杠杆为 `120000` 张，25× 降为 `55000`，45× 再降为 `28000`；“升杠杆必增容量”的假设不成立。现行硬门仍按工单要求在非单调时阻断，不截断数量；最新错误会耐久保存触发点，例如 `20x=120000 → 25x=55000`。两次自然多单分别需要 `441374`、`436449` 张，均无法在当前真实容量内执行。
- 2026-07-24 `WO-EXEC-03` 新一轮真实 Demo-S 回归已闭环。第一次 execution `622b53fa-6d53-547f-ab4f-bf8ed3e2c9c6` 的真实 OKX Demo 入场限价单 `3770752241751908352` 在 270 秒内成交 `0`，系统按规则撤单并落为 `canceled`，没有切市价。第二次 execution `cf351b86-9f24-59b5-9814-ab203e0fbb19` 由真实 SupervisorGate 放行，唯一 Worker 提交并成交 `111596` 张多单，入场订单 `3770781223721459712` 均价 `4052.1281972472131626`；两张原生 OCO 各保护 `55798` 张，主动离场前全部确认撤销；减仓限价单 `3770785337595486208` 最终全成，均价 `4049.0276640739811485`，执行账本 `closed`、剩余 `0`、已实现盈亏 `-292.8682232087172257`。收口只读回查为空仓、普通挂单 `0`、八类算法挂单 `0`；这只证明 Demo 技术链，不是自然 PA 绩效。
- 2026-07-24 上述真实反馈推动最小修复：Worker 对账与 Controller 入队交错时不再把普通 `PENDING` 命令误判为未解决券商写入；Campaign/Demo-S 的监督、风险和状态区分阶段二信号价与 `0.50 × ATR14` 后的最终有效限价，受控可成交回锚会反推信号价而不是重复加滑点。Demo-S 离场等待改用 330 秒，并在异常减险后等待真实终态；Campaign 监控会把较新的 execution 终态同步到状态显示。Codex CLI 版本探测成功路径按客户端缓存，探测窗口为 10 秒；候选不存在、临时探测超时和确定不可用三种语义分离，临时超时只跳过当前 K 线。
- 2026-07-24 现役 Campaign 已在空仓硬门下重建为 `7c462fdc-0574-4185-ad8c-1383bbeb77aa`，配置指纹 `9a9b20be6a7fee5b1a39af8f161d6d89ef7ad47056fcd9c1c3dfffce0140f954`；固定配置仍为 `OKX Demo / XAU-USDT-SWAP / 10m / min_trade_confidence=20 / extreme_aggressive / entry+exit limit_with_slippage 0.50×ATR14`。新 Worker `cc64b833-81ee-4bad-b252-a2b79f3d97f2`、实际 PID `39088` 已完成首次和后续对账，错误码为空；重启继承 `last_completed_bar_ms=1784881800000`，没有重复分析上一根。活动 execution/未解决写命令/租约、仓位和挂单均为 `0`。这些仍是现场快照，后续必须实时核验。
- 2026-07-24 风险停止解除后的首笔受控 Demo-S 暴露并修复 Worker 并发竞态：execution `7e9155da-991c-55c3-a519-0b050ab6b118` 已由真实 SupervisorGate 放行并创建 `110683` 张计划，但 submit `f38693bb-2623-4cf7-b19e-728e3e717164` 在 Worker 领取前被错误标为 `failed:worker_needs_attention`，因此券商订单号、成交量、仓位和挂单均为 0，不能算交易。根因是周期对账结束时 `_set_available_heartbeat()` 把正常 `PENDING` 写命令也当成未解决券商写入，主动进入 `NEEDS_ATTENTION` 并撤销其合法租约。现已把健康阻断收敛到崩溃遗留 `RUNNING` 和无耐久裁决的 `UNCERTAIN`；正常 `PENDING` 保持 Worker 可用、保留租约并由下一轮领取，同时保留原有不确定写入失败关闭。
- 2026-07-24 用户已明确把“OKX Demo 常驻交易与反馈闭环”升级为 PA_Agent 项目级规则：开发、测试、文档和审查期间仍须保持 Worker、Campaign 和只读运行监控；自然信号有效时必须经真实监督与 `Controller → Worker` 进入完整交易生命周期，自然 `no_order` 只记录真实原因并继续下一根 10m K 线。只要本阶段修改或验收执行链，就不能因连续 `no_order` 整阶段没有真实 Demo 反馈，应及时使用明确标记的 `controlled_reproducible` 输入完成真实监督和完整执行链验收，不得伪装成自然策略绩效。项目规则真身为根目录 `CLAUDE.md`，`AGENTS.md` 只负责指向该规则入口。主 Agent 工作期间至少保留一个只读运行监控子 Agent，复杂阶段结束再做对抗审查；子 Agent 仍禁止券商写入、修改租约、启用 Live 或触碰 AlphaMaster。
- 2026-07-24 用户显式授权解除一次 `BrokerTransportError` 触发的持久风险停止。`ExecutionController` 已排队控制命令 `729d5331-37a5-434e-81e4-52d314ef1d92`，唯一 `ExecutionWorker` 执行结果为 `succeeded / clear_drawdown_stop_completed`；清除前无 `NEW_RISK` 租约，清除过程不创建订单。Worker 成功重新读取账户后把 `last_total_equity_usd` 与 `adjusted_high_water_usd` 同时重锚为 `78970.87234383462`，`drawdown_fraction=0`、`kill_active=false`。这只恢复 OKX Demo 新增风险能力，不构成交易信号。
- 2026-07-24 `WO-RUN-06` 与动态杠杆后续已部署到本机真实运行链：`records/execution_control.sqlite3` 已在唯一 Worker 持锁下从 schema v1 迁移到 v4；迁移前一致性备份为 `records/backups/execution-control-v1-before-v4-20260724-135141.sqlite3`，SHA-256 `1516981E36A8D16411C36326987669C6AAA40AD0BC299636A46B9829E9C392BF`，不可变只读完整性检查为 `ok`。风险运行态已建立可信 `account.totalEq` 基线和七天 OKX 账单扫描边界；新增风险与调杠杆在券商写入前都刷新权益/账单并执行 `>=50%` 回撤停止。首次建立基线时，基线之前的历史转账已包含在当前总权益中，只记录扫描边界、不重复调整高水位；只有边界之后的新外部资金流才参与校正。Worker PID `48324` 启动首轮曾遇到一次 `BrokerTransportError`，按工单 fail-safe 规则把新增风险停止置为 active；权威读取恢复后系统没有自动解除，直到用户显式授权、Worker 成功执行 `CLEAR_DRAWDOWN_STOP` 才重锚并恢复新增风险。杠杆计划必须在有界策略网格逐点读取容量、由真实监督授权，并只经 Controller→Worker→Service→OkxAdapter 写入和回读；容量不单调或最大杠杆仍不足时继续阻断，不截断数量。
- 2026-07-24 远程发布补漏已完成：首个 WO-EXEC-03 收口提交漏掉了文档已引用的 GUI Demo/Live 账户路由、90 秒快照陈旧语义和内部换币验收文件；现已用精确 3 文件提交 `17e836d69530850cf0b7da235707b211536506f5` 推送 `origin/main`。对应 14 个测试通过、0 失败，staged gitleaks 0 泄漏；价格行为、多周期和提示词的并发工作区改动未混入。补充工单硬验收 66 个测试通过、0 失败，新增覆盖 Demo-S 逐态证据、HTF 不改硬决策/定仓、非法 ATR 无回退和 canary 限价撤单不切市价。
- 2026-07-24 原始价格行为资料第二轮复核已完成：对照 `E:\QQ文件\价格行为学资料` 的 Brooks 识别文本，确认“少量相关周期、高周期解释背景与位置、主周期独立负责交易、不用低周期拼触发理由”。已修正二元决策、市场诊断、K线信号、通道、尖峰和二次入场提示词中残留的多周期共识/高周期可靠性硬闸；`1h/4h` 只输出方向、结构、位置、ATR14 薄标签，仍不自动改置信度、数量、价格或 gate。相关多周期、GUI、OKX Demo、提示词定向套件 `107/107` 通过、0 失败；原始 PDF/OCR 未复制到公开仓库，Session/成交量未升级。
- 2026-07-24 当前正式配置已统一为：`OKX / XAU-USDT-SWAP / 10m / extreme_aggressive / min_trade_confidence=20`，入场与主动离场均为 `limit_with_slippage`，滑点为各自 `0.50 × 主周期 ATR14`；`10m` 不是 OKX 原生周期，而是两根连续、同一 UTC 10 分钟边界且均已收盘的官方 `5m` K 线严格聚合。高周期继续使用 OKX 原生已收盘 `1h/4h` 薄背景，只解释结构，不机械否决主周期；Session 与成交量本轮不变。GUI 已持久化并显式显示行情源、Demo 环境、进出场模式、ATR 倍数和高周期背景，OKX 执行拒绝接收 MT5 价格源。
- 2026-07-24 `WO-EXEC-03` 已完成三种真实 OKX Demo 模式覆盖。Demo-A execution `4d118cb0-ec65-5340-a8c9-9f9d8583bd1e` 以 `limit → market` 完成 `27440` 张入场、两笔原生保护和按 OKX 实时 `maxMktSz=20000` 拆成 `20000 + 7440` 的两笔市价减仓，最终 `closed`、剩余 `0`、已实现盈亏 `-73.5046000000000050`。Demo-C execution `89e45ac4-4bee-58eb-9686-7ac36f90db79` 以 `market → limit` 完成 `15057` 张入场、两笔原生保护和一笔完整限价主动离场，最终 `closed`、剩余 `0`、已实现盈亏 `-117.0923`。收口只读回查为非零仓位 `0`、普通挂单 `0`、条件/OCO 挂单 `0`、活动 execution `0`；Demo-S/A/C 合计恰好覆盖入场与主动离场的 `limit_with_slippage`、`limit`、`market` 三种模式。它们只证明 Demo 技术链路，不是 PA 策略绩效。
- 2026-07-24 Demo-A/C 风险数量已按耐久账本复算：`ctVal×ctMult=0.001`、`lotSz=minSz=1`、风险比例 `10%`、费率 `0.0005`、保守滑点率 `0.0010`。Demo-C 使用入场前空仓账户快照权益 `8714.71942600003`，精确得到 `15057` 张；Demo-A 当时缺少紧邻入场前的直接账户快照，只能用成交后首份账户现金 `8843.74440770003` 加已扣入场费用 `55.557134` 重建权益 `8899.30154170003`，精确得到 `27440` 张。历史两笔的确切 `maxBuy/maxSell` 没有耐久保存，不能事后伪造；现已让所有后续 Campaign/Demo 验收记录在监督与计划前耐久保存权益、10% 预算、三价风险、费用/滑点、`minSz/lotSz`、`maxBuy/maxSell` 和最终数量。
- 2026-07-24 Demo-A/C 实证发现并修复两处生产缺陷：OKX 市价减仓现在按实时单笔 `maxMktSz` 分块，但绝不截断总风险数量；主动离场的部分成交按“下单前剩余仓位 − 券商累计成交量”计算，不再每轮重复扣减。另补上保护撤单未知恢复：只有券商权威查询确认原保护单仍为 `live`，才先耐久生成新撤单意图，下一轮再次复核后由唯一 Worker 写券商；没有盲目重发。共享环境的 `PA_AGENT_LIVE_TRADING_ENABLED` 与 `OKX_LIVE_ENABLED` 已明确关闭，模拟门保持开启。
- 2026-07-24 已重新读取用户提供的网页版 GPT 共享对话并核对正式资金规则：资金转入、转出和账户划转不能计为交易盈亏，也不能虚假抬高资金调整后的历史最高净值；资金变化本身不是交易信号。当前代码和生产控制库 v4 已把最近七天 OKX 账单连续扫描边界、USDT `type=1/subType=11/12` 外部转入转出、`account.totalEq` 高水位、50% 新增风险停止和人工 Worker 重锚接入；旧转账自然滑出七天窗口不会误停，连续扫描真正中断满七天才失败关闭。Funding 账户充值/提现、基线建立后的非 USDT 外部转账时点换算和七天以上停机补链仍未实现，这三类情况继续失败关闭，不能伪造完整资金流。
- 2026-07-24 用户本轮现场确认是“其他资产换成 USDT / 账户内转换”，不是外部转入。真实 OKX Demo 只读复核显示 USDT `eq/cashBal/availBal=79041.2190279924`、`frozenBal=0`、账户 `totalEq=78976.40522916657`；最近账单中最新换币为 `type=2/subType=1` 的 `USDC-USDT`，时间为 `2026-07-24T03:06:31.671Z`，历史唯一 `type=1/subType=11` 外部转入为 `2026-07-17T08:04:08.320Z` 的 `5000 USDT`。本轮已修复：无活动 execution 时 Worker 仍刷新选定账户；GUI 正确读取 `okx-demo` 并把超过 90 秒的快照标为 `UNKNOWN`；OKX 快照保存 `totalEq/USDT eq/cashBal/availBal/frozenBal/uTime` 来源；Campaign 明确以 `usdt_equity` 为 10% 风险基数；提交前重新定仓，余额或风险输入变化时旧 `READY` 计划直接作废，不提交。当前 10% 风险公式仍遇到真实 `max_size_exceeded` 就硬阻断，不静默截断；生产资金流游标和高水位已经随控制库 v4 部署。一次已恢复的只读传输故障曾触发 fail-safe 停止，现已按用户显式授权由 Worker 重锚解除。
- 2026-07-24 `WO-EXEC-03 Demo-S` 已完成真实 OKX Demo 单仓闭环：受控输入显式标记 `controlled_reproducible`，真实 SupervisorGate 使用 `codex-subscription / gpt-5.6-luna / medium` 返回 `allow_entry`；execution `328d0f7f-fef6-5e5e-bd39-dad92a66512b` 通过 `ExecutionController → ExecutionWorker → ExecutionService → OkxAdapter` 做空 `14921` 张，入场订单 `3769893223932182528` 全部成交，建立两笔原生保护后按主动离场流程撤销保护，离场订单 `3769895836211822592` 全部成交，账本最终 `closed`、剩余数量 `0`、已实现盈亏 `-21.3546079887406998`。收口只读回查为非零仓位 `0`、普通挂单 `0`、条件/OCO 挂单 `0`；这只证明 Demo 技术闭环，不是自然 PA 信号或 Live 绩效。
- 2026-07-24 旧 Campaign `52a4f507-a8f8-4975-b0bf-ddddf9ff901c` 已归档，Campaign `0d239206-c7bc-436c-9944-23e9433e34d5` 已按 `10m / 20 / extreme_aggressive` 启动；主周期来源仍是严格 OKX 5m→10m 聚合，进出场均为 `limit_with_slippage / 0.50 × ATR14`，高周期为 1h/4h 薄背景。该条只记录恢复时首根 `blocked:no_order` 的历史快照，不代表当前运行态；之后已有受控 execution 和 Worker 并发竞态证据，现役 Campaign 状态必须按进程、状态文件和账本实时复核。旧 execution `8c0f83ab-fc6b-589e-8967-c4bd8f538015` 保持 `canceled`；其历史 `uncertain` submit 命令已用当前 Demo 账户身份、`plan_created → ready_expired` 事件和券商空仓空单证据裁决为 `confirmed_not_written_schema_validation`，未解决 uncertain 写入现为 0，没有盲目重提。
- 2026-07-24 失败语义已补强：`max_size_exceeded` 记录为 `blocked:risk:max_size_exceeded` 并继续下一根已收盘 K 线；回撤/资金流停止对新开仓写入 `risk_runtime_blocked` 事件，payload 同时保存稳定错误码、当时 `drawdown_fraction` 和 `adjusted_high_water`；Worker 在券商调用前发现执行记录 schema 不兼容时明确返回 `failed:execution_record_invalid`；Demo 生命周期等待器允许 Worker 单次全局对账异常后继续读取执行账本，但 execution 自身进入 `needs_attention/unknown/error` 时仍立即硬阻断。九种入场/离场组合已通过真实生产链离线 Fake client 验收，ATR `2→4` 的价差翻倍、风险滑点率不改委托价、保护与主动离场模式隔离均有测试覆盖。
- 用户当前 GitHub 用户名为 `xiaojinlucky`；已明确要求 `xiaojinlucky/PA_Agent` 保持 `PUBLIC` 并公开发布，因为网页版 GPT 可以读取私有仓库，但 BioMNI 读取本项目依赖公开访问。给网页版 GPT 或 BioMNI 的启动指令必须明确这一点，不得要求改回 private。本地 `origin` 已改为规范地址 `git@github.com:xiaojinlucky/PA_Agent.git`。上一次交接包实时核对确认登录账号为 `xiaojinlucky`、权限为 `ADMIN`，当时本地 `main`、`origin/main` 与 GitHub API 返回的基线 SHA 均为 `38876644430302f0e2ac3310f072b31f95252469`；本轮提交/推送状态必须重新按 Git 实时核验。交易执行与大模型供应商接入已纳入该发布基线；环境文件、密钥、数据库、日志和运行态记录一律排除。
- 2026-07-23 历史基线曾冻结为 `15m` 主周期 + `1h`/`4h` 薄背景；该主周期已被 2026-07-24 的 `10m` 正式决定取代，资料对“嵌套结构、少量相关周期、高周期只作背景”的约束继续有效。
- 2026-07-23 历史实现已完成入场和主动离场独立三选项及 ATR 滑点；2026-07-24 现役 Demo-S 配置进一步固定为进出场均 `limit_with_slippage / 0.50 × ATR14`，主周期改为严格聚合 `10m`。
- 2026-07-23 本轮离线验证：多周期/ATR/提示词/适配器/运行器定向套件 `209` 通过、`0` 失败；ExecutionService/Controller/Worker/Store/生命周期回归 `97` 通过、`0` 失败；`compileall`、改动范围未定义名/语法检查和 `git diff --check` 通过。新版 Campaign `e396d8eb-e3bf-498a-83c6-8654c0528fbb` 已启动，但当前被本机 GUI 会话持有的 `NEW_RISK` 短租约阻塞在首轮分析前，尚未产生本轮新的真实订单证据；不撤销 GUI 租约、不把等待说成交易。普通 GUI 行情源尚未接入自动高周期读取，本轮先把真实 Demo 主运行路径打通。
- 2026-07-23 `WO-RISK-02` 已完成本机适配：新增纯 `Decimal` 风险计算器，按 Demo USDT 权益的 10% 风险预算、PA entry/stop、`ctVal`/`ctMult`、费用、滑点、`lotSz`/`minSz` 和方向最大可开张数计算首仓张数；风险结果在现有 `ExecutionController → ExecutionWorker` 链路中进入 `ExecutionPlan.quantity`。已有新增风险租约时，定仓改量会先撤销旧租约并按新配置指纹重新授权。没有 PA entry/stop 时只做只读预检、不计算数量、不回退 `equity_10pct_notional`；本轮未修改 `pa_agent/execution/`，未连接真实券商，未发送 Demo/Live 订单。
- 用户已重新授权前端改版，但明确要求先由 Gemini Stitch 设计。`docs/FRONTEND_REDESIGN_PRD.md` 已完成，生产视觉改版、Pencil 状态流和 Figma 组件库尚未完成；OmicOS 只参考“订阅登录/API 接入”分组与信息层级，不照抄品牌风格。
- 已把交易写入从 GUI 与自动循环中移出：`ExecutionController` 只创建计划、发放短期新增风险租约和写入持久命令，单实例 `ExecutionWorker` 是唯一构造 `ExecutionService`、连接券商和执行命令的进程。命令状态、后台心跳与最后成功对账分别落在 `records/execution_control.sqlite3`，交易真值仍只有 `records/execution.sqlite3`；减险写入不依赖新增风险租约，但继续受环境硬门、不可变账户路由和持久停写标记约束。生产账本检查时已是 schema v2，因此没有重复迁移；已先创建一致性备份。WinSW 服务已安装为 `LocalService`、自动延迟启动并保持运行。Live 开关和容量不是本轮证据，不能据历史记录断言已打开或关闭；本轮只验证 Demo/离线链路，未提交 Live 订单。GUI 检测到该服务时只请求 Windows 启动服务；除系统明确返回服务不存在（1060）外，服务控制程序缺失、超时、权限或状态不明均禁止备用 Python Worker。
- Longbridge 行情数据源仍仅使用 `QuoteContext`；三个交易档案分别创建独立上下文。模拟账户绝不回退实盘且不允许盘前/盘后；日内账户只在提交前明确最大数量不足时回退综合账户，任何网络/认证/未知/已提交状态均不回退。
- OKX 不硬编码黄金；动态规格测试覆盖 `XAUT-USDT`、`XAU-USDT-SWAP`、`BTC-USDT`、`BTC-USDT-SWAP`。现货与永续有独立数量、保护和盈亏语义。
- 所有真实写操作要求 `PA_AGENT_LIVE_TRADING_ENABLED=true`、当前进程会话确认；OKX Live 还要求 `OKX_LIVE_ENABLED=true`。Longbridge 与 OKX Demo 的模拟写操作改用独立 `PA_AGENT_PAPER_TRADING_ENABLED` 和 `启用模拟交易`。历史记录曾记录 Live 开关和容量，但本轮未重新核验；Demo 5 分钟自动循环的提交记录属于历史证据，不能替代本轮 WO-S2A-01 的离线验收。
- 2026-07-23 02:47，旧版 5 分钟 Demo 运行器已完成 7 笔 `10` 张限价空单的真实 OKX Demo 提交；7 笔均被券商接受、成交量均为 `0`，在 `270` 秒内未回到挂单价后均已撤销，当前无仓位。为验证即时执行路径，已将运行器专用阶段二提示切为 `market_when_valid`：仅当能在最新已收盘 K1 附近构造合法三价时输出市价单，否则如实不下单；该规则不影响 GUI 和日常 PA 分析。新 campaign `c928e2bf...` 已连续完成 8 根 5 分钟 K 线的真实分析（0 失败、0 新 execution）；均因尖峰追单/下沿支撑/无回撤确认或区间转换被模型判为不下单。Runner、`ExecutionWorker`、心跳、账本和 OKX Demo 私有回查一致且正常。策略运行器短暂停止后，已通过同一 Controller/Worker 链路完成独立 `okx_demo_lifecycle_canary`：Demo `10` 张市价入场、成交回读、两笔原生保护、受控离场，最终 `closed`、剩余 `0`，Demo 无非零仓位；随后策略运行器已恢复。
- 2026-07-23 22:26-22:41 的现场运行已重新核验：旧 Campaign 在唯一 `NEW_RISK` 租约到期前 7 秒启动，因租约竞争直接结束且只完成 `0` 根分析；本轮新增 `NewRiskLeaseUnavailable` 类型错误，Campaign 把这一个明确的短暂租约竞争放回循环重试，硬门关闭和其他真实错误仍直接失败。修复后的定向 Campaign/Controller 测试为 `55` 通过、`0` 失败，`compileall` 和 `git diff --check` 通过。
- 同一现场按用户授权完成一次真实 OKX Demo 生命周期 canary：execution `3f57bed9-6997-58ff-9e6f-e3b6cd1554c2`，`15358` 张市价入场，成交回读后建立两笔原生保护，主动离场成交，账本最终 `closed`、`needs_attention=false`；随后 OKX Demo 只读回查为净仓 `0`、普通挂单 `0`、待生效算法单 `0`。该 canary 明确标记为 `okx_demo_lifecycle_canary`，不计入 PA 策略绩效。
- 历史 15 分钟 Campaign `d6936271-7d86-49b7-8e94-e069ed5bac2f` 已由当前 10 分钟 Campaign 取代；该条只保留为旧现场快照。
- `docs/GPT5_6SOL_HANDOFF.md` 与 `docs/LOCAL_EXECUTION_CONTEXT.md` 是开发前历史快照，已加醒目标记；当前实现真值以本文件与 `docs/LIVE_TRADING_DESIGN.md` 为准。
- AI 模型首轮范围已按用户最新决定收敛为 Codex ChatGPT 订阅、Kimi API、DeepSeek API；小米 MiMo 暂不纳入本轮可用性验收。当前配置有 Codex Luna、Codex Terra、Kimi、DeepSeek 四个已验证档案，活动档案为 `codex-subscription` / `gpt-5.6-luna`；Luna 与 Terra 是同一 Codex 订阅通道下的两个模型档案，不是新增供应商。
- Codex 登录故障根因是程序误选了 WindowsApps 中存在但不可执行的无后缀资源。现在每个 `.exe` 候选都必须实际通过 `--version`，当前使用 `C:\Users\Administrator\AppData\Local\OpenAI\Codex\bin\codex.exe`；ChatGPT 登录状态和独立随机挑战均通过。
- 模型下拉框采用“基础目录 + 当前账号刷新”：刷新成功后只展示账号接口返回模型，基础目录只补同 ID 能力；刷新失败保留原列表。2026-07-20 真实目录为 Codex 6、Kimi 12、DeepSeek 2，当前三个选中模型都在各自账号目录内。
- API Key 显示/隐藏已修复，切换供应商或档案会强制重新遮罩；模型目录任务按适配器、地址和 Key 哈希隔离迟到结果。Thinking、推理强度、速度与上下文行按模型能力显示，未知上下文全链路保持 `None` / “尚未确认”，目录能力变化会强制重新测试。
- 上下文上限已改为模型目录驱动的只读元数据。Codex 原生持久线程与恢复用于自由追问，并允许官方客户端在达到模型阈值时自动 Compact；正式两阶段分析仍是无历史复用的一次性请求。线程恢复已验证，尚未用超长真实会话触发并观测一次 Compact 事件。
- Codex、Kimi、DeepSeek 三条正式通道已完成真实随机挑战；当前四个档案的持久验证指纹均有效。活动档案后来由用户配置为 `codex-subscription` / `gpt-5.6-luna`，Terra 档案仍保留并有效；API Key 不写入仓库配置，而是按需从仓库外共享环境读取。
- 已定位用户将 Codex 从 Sol 改为 Terra 后“测试通过但激活失败”的直接原因：日志在 2026-07-20 11:14 两次记录 `SettingsConflictError`，不是登录或 Terra 模型无效。模型测试期间普通设置已推进磁盘 revision，旧候选因此被整体拒绝。现在 revision 判断、快照读取、AI 状态比较、合并与原子替换处于同一临界区；进程内 `RLock` 串行线程，文件锁串行外部进程。磁盘 AI 档案未被并发修改时保留最新普通设置并安全合并已验证候选；另一窗口也改 AI 档案时仍失败关闭。账号实时目录确认 `gpt-5.6-terra` 存在，独立真实随机挑战四项全部通过。该修复完成后 Terra 曾成功激活；用户后来又把活动档案改为 `gpt-5.6-luna`，当前持久配置 revision 为 61。

## 历史现场验证快照（截至 2026-07-22/23；本轮未重新核验）

> 本节记录上一轮现场核验结果；本轮只新增 WO-S2A-01 的 Demo/离线验证，不重新证明这些运行态结论。

- 2026-07-22 交易执行、OKX/Longbridge、数据源和交易窗口相关分组为 374 通过、0 失败；Codex/Kimi/DeepSeek、档案、目录和模型能力相关分组为 247 通过、0 失败；四个 Qt E2E 为 4 通过、0 失败；取消程序擅自扩大止损的直接风险指标回归为 51 通过、0 失败。全仓现状为 1251 通过、32 失败、3 跳过；其中 4 项是 AkShare 真实联网超时，27 项经两名独立审查者确认属于 HEAD 既有测试契约/旧输入而非本轮回归，1 项日志遮罩性质测试是日志为空而非明文泄漏。不能把全仓描述为全绿。
- 生产 `records/execution.sqlite3` 已是 schema v2，`quick_check=ok`；截至 2026-07-22 有 11 条历史 execution。历史 OKX Demo execution `cc94657a-d4b3-5ba3-b4d4-0d6fd62ae595` 的保护单经精确查询、未触发列表和三个月历史确认不存在；系统没有自动重发保护。2026-07-20 使用持久命令 `08c2e0a4-4eec-447e-8d22-f9921f95defa` 只执行减险离场，命令为 `succeeded`，execution 已为 `closed`、剩余数量 0、`needs_attention=false`。2026-07-23 的 5 分钟 Demo 循环已完成首次 AI→计划→Worker→OKX Demo 提交：第一笔 10 张限价空单因旧的 120 秒超时未成交而撤销，随即把快速循环的入场有效期改为 270 秒。新运行器随后提交第二笔 10 张限价空单，先越过旧的 120 秒节点保持 `live`，随后在约 270 秒后未成交撤销；券商只读回查与账本一致。后续成交、保护和离场以执行账本的实时状态为准。2026-07-22 又补齐算法单未触发/历史接口的完整分页：只有精确查询和全部分页都成功且没有命中时才能标为 `confirmed_absent`；第二页失败、游标缺失/重复或超过安全上限均保持未知并禁止自动补发。安装前备份大小 1,155,072 字节、SHA-256 为 `cd8e4652a37b54c2c6ae52b7233307e1efae46ac27e2b395b271e56423a8e771`，已复核为生产账本的精确历史前缀。
- WinSW 服务当前为 `Running`、自动延迟启动、`NT AUTHORITY\LocalService`；2026-07-23 Worker 心跳与最后成功对账持续更新。用户已明确授权关闭 Demo 中不属于 PA 的 `XAU-USDT-SWAP` 10 张净多仓，市价平仓后只读回查确认该外部仓已不存在。Demo 自动循环现在独占 PA 自己创建的订单与执行账本，不接管外部仓位。
- 权限目录层和旧文件已按“代码/WinSW 只读执行、共享 env 只读、records/logs 可写”收紧。管理员脚本先修复了隐藏 PowerShell 不支持终端录屏、Windows PowerShell 5.1 不支持换行管道两项兼容问题，于 2026-07-20 16:00 成功完成；`pa_agent`、`records`、`logs` 中保留 `Authenticated Users: Modify/Write/FullControl` 的文件复扫为 0。GUI 不承载执行 Worker；WinSW 服务与 GUI 保持独立进程边界。2026-07-22 实时复核服务仍为 `Running`、自动启动，账户仍为 `NT AUTHORITY\LocalService`。
- Longbridge 综合、日内和 paper 三档的账户/行情/容量结论均是历史只读证据；本轮未重新核验。OKX Demo 私有读和动态品种结论也不替代本轮测试；OKX Live 状态待现场重新核验。
- Codex、Kimi、DeepSeek 三条通道均通过真实随机挑战；当前活动模型后来改为 Codex Luna，Terra 仍为另一个已验证档案。Codex 已完成一次隔离的 PA 两阶段真实分析，未连接执行服务、未下单。模型目录、API Key 遮罩、按模型显示能力、只读上下文上限和 Codex 持久追问线程均已实现；用户可在 Luna/Terra 之间切换，但纯手工点击链路仍由用户完成，Codex 不控制鼠标。
- Terra 激活并发回归与设置持久化回归共 59 通过、0 失败；静态未定义名/语法检查通过。独立只读复验六维全部通过、阻塞问题 0。更宽的 AI 现状套件另有 1 个与本次无关的既有 `openclaw_cs` 路由失败，未纳入本次 Terra 修复。
- OKX Demo 巡检已修复价格步长浮点尾差、程序改写模型止损、恢复门禁、过期 `READY` 计划补提、保护单权威查无和 Worker 心跳覆盖新状态的问题；自动循环始终限定 Demo、`XAU-USDT-SWAP`、5m、cross、10 张，限价入场有效期 270 秒。历史仓位已经按上述减险命令安全收口。
- MT5 是否显示形成中 XAUUSD K 线取决于券商当前是否开市；2026-07-20 周末无新 tick 时不画过期虚线属于正确行为，仍需开市后可见复验。上游仅 AkShare 字符串时间解析修复值得以后小范围移植，不能整体合并。

## 已知边界

- OKX Demo 私有只读链路和 5 分钟自动循环属于历史运行证据；本轮代码只允许 Demo/离线验收。Live 状态必须每次按现场配置、实时容量和券商预检重新核验，不能从旧文档推断。
- Longbridge 两账户当前 GLD.US 可交易数量为 0；在账户资金/资格变化前，真实预检会阻断。
- Longbridge paper 的撮合和现金规则与实盘不同，且美股只支持常规交易时段；模拟结果不能替代综合/日内账户的真实可交易验收。
- Longbridge Legacy Token 更新时必须来自同一绑定账户；类型或账户 ID 不一致会在创建交易会话前失败，不能通过修改档案名称绕过。
- Longbridge Legacy Token 到期仍需人工更新；账户总盈亏接口没有可靠的已实现/未实现拆分，PA 不伪造拆分。
- Longbridge 止损是券商端原生 MIT，止盈条件由 PA 软件轮询；关闭 PA 后原生保护仍在，但软件止盈和状态回写暂停。OKX 保护使用券商端 OCO。
- 最小真实 Canary 未获本轮授权；后续必须对具体券商、账户、品种、方向和数量重新单独确认。
- Codex 订阅不产生单独的 OpenAI API 按 Token 账单，但仍受 ChatGPT 套餐用量和频率限制；PA 以纯文本、禁工具、禁技能说明、临时空目录和清理后的环境调用，不读取或复制 Codex 登录凭据。
- PA 后端信息架构只有在提交边界核清并安全推送后，才交给网页版 ChatGPT 通过 GitHub 只读审查；仓库公开性已由用户明确接受。网页版工单仍必须由本机 Codex 按真实 skills、memory、运行态和交易安全边界校正后才能执行。
- 用户给出的网页版 ChatGPT 项目对话需要登录态；本机内置浏览器当前被引导到登录页，因此本轮无法重新读取原文。已有 PRD/加固计划只可视为之前整理出的结果，不能冒充本轮已经重新核对过该对话。
- 当前代码和 WinSW 验收不等于可以打开实盘。数据库备份、schema v2 核对、独立守护、GUI/Worker 进程树隔离、旧运行态文件 ACL 和历史 Demo execution 安全收口已经完成；更完整的券商启动扫描、持续持仓/保护真值核对、Longbridge 私有推送与全局限速仍未完成，因此不能宣称长期无人值守实盘已完成。
- 当前持久交易配置经本地 `config/settings.json` 核对为 revision `71`：OKX Demo、`XAU-USDT-SWAP`、严格聚合 `10m`、`extreme_aggressive`、决策与执行最低置信度均为 `20`、进出场均 `limit_with_slippage / 0.50 × ATR14`。数量不再把持久固定值当风险真值，每次候选计划按实时权益、止损距离、合约规格和最大可开数重新计算。
- 2026-07-23 已按用户授权关闭原有的外部 Demo 10 张仓位；当前由 PA 自动循环创建、监控和收口自己的订单，不再存在阻塞同品种新计划的外部仓位。

## 上一轮接手现场快照（2026-07-23 UTC；本轮未重新核验）

- 上一轮曾把 PA 模式固定为 `aggressive / 30 / 15m`；该历史配置已被 2026-07-24 的 `extreme_aggressive / 20 / 10m` 正式决定取代，仍只限定 OKX Demo，不改变任何 OKX Live 路由。
- 在启动新运行器前已完成只读核验：Demo 无非零 XAU-USDT-SWAP 仓位、无普通挂单、无 OCO/触发保护单；本地无活动 execution/命令；`PAAgentExecutionWorker` 服务运行且心跳、最后成功对账均正常。实时预检成功，最近一次按约 5,000 USDT 权益、约 4,136 USDT 价格解析为 `120` 张；这是容量计算结果，不是策略下单。
- 上次只读快照记录 Campaign `6c7bc424-6d00-4bd8-9c67-fca1cfa62b39` 于 `2026-07-22T20:12:56Z` 启动，并完成前两根 15 分钟 K 线真实两阶段分析（2 成功、0 失败、0 strategy execution）：第一根交易置信度 45、价格在下方支撑附近、信号 K 线无效且没有可定义的即时止损；第二根交易置信度 35、外包阳线没有确认跟随。两次订单类型都明确为“不下单”，Controller 均因 `no_order` 正确未创建计划。这不是漏单，也不是 canary 或策略绩效；本轮实时状态探针超时，当前是否仍 active 待重查。
- 自动巡检 `okx-24` 已迁移为“PA Agent OKX Demo 5 分钟只读巡检”，绑定当前 PA 审查线程，到本 Campaign 到期前只读检查 Campaign、进程、Worker、账本和 Demo 仓位/订单/保护摘要。它不得写券商、改配置、启停进程、运行 canary 或修复代码。
- 2026-07-23 已完成统一路线图阶段 0 的压缩真值冻结：阶段 0 发布基线为 `38876644430302f0e2ac3310f072b31f95252469`；三份外部 PRD 原文归档到 `docs/prd/`，并明确“当前能力以代码/当前证据为准、目标行为以已确认 PRD 为准”；`docs/ROADMAP.md` 已切换为无界面主脊柱优先路线，`docs/SAFETY_INVARIANTS.md` 和 `docs/BASELINE_AUDIT.md` 继续提供安全和状态边界。阶段 0 未修改 `pa_agent/execution/` 下任何 Python 行为，未执行交易；历史验证证据保留原 SHA 并加历史标记。本轮提交/推送状态以 Git 实时核验为准。
- 2026-07-23 已推进统一路线图阶段 1 的第一块可验收实现：`pa_agent/gui/read_models.py` 只读组合行情连接/订阅、Worker 心跳与对账、账户快照和执行账本；`AppContext` 复用现有两个 SQLite 存储，主窗口状态条显示每个字段的“已确认/计划/规划/未知”语义，数据源事务切换后读取对象同步更新；字段映射见 `docs/READ_MODEL_MAPPING.md`。读取层不发网络请求、不创建计划、不写券商；完整导航页面、截图和 OKX 端到端链路验收仍未完成。读取层 3 项、主窗口状态 2 项、执行控制器/Worker 33 项、账本/服务/生命周期 56 项定向测试已随当前发布基线验证通过。
- 2026-07-23 已新增 GitHub 交接总账 `docs/CODEX_HANDOFF.md` 和网页版 GPT 启动指令 `docs/WEB_GPT_CONTROLLER_PROMPT.md`，用于把本项目规则、历史需求、已发现问题、已完成修改、未完成事项和硬验收边界交给外部总控；它们不包含凭据、数据库、日志或运行态文件。
- 2026-07-23 已推进阶段 1 首张工单 `WO-S2A-01`：新增严格 `SupervisorDecision`、不可变监督输入快照、主/备监督模型和确定性 `block_entry`，监督结论按 Campaign + 已收盘 K 线 + PA 分析摘要原子落盘；同一结论重启复用，拒绝不调用 `prepare_analysis()`，放行继续使用现有 `ExecutionController` / `ExecutionWorker`。角色档案已增加 PA 主/备、监督主/备四个绑定字段；当前 Campaign 实际使用 PA 主档案和已配置的监督主档案，监督备用档案只有在本机设置明确绑定且验证通过时才启用，空绑定直接确定性阻断，PA 备用档案先做独立验证并留给后续 PA 工单。新增离线硬验收使用真实 Controller/Worker 加内存 FakeAdapter，证明第一根 K 线拒绝时命令数为 0，第二根放行时恰好一条 Demo `SUBMIT`，重启不重复模型调用或命令；本轮未连接真实券商、未发送真实订单，`pa_agent/execution/` 未修改。
