# PA_Agent Context

## 当前状态（2026-07-16）

- 私有仓库：`Jinqingchang/PA_Agent`。多模型与 Longbridge 只读行情功能发布提交为 `3d9353f8579e6d661fd314ab6b9e91016d9fdd96`，已推送到 `main`；最终仓库 HEAD 以 GitHub `main` 与 PA_Agent 独立移交包清单为准。
- 用户已终止全部 PA_Agent 前端重设计。生产代码已恢复原单行控制栏、原字号/间距/组件尺寸、原主题和全部免责声明；Longbridge 状态仅保留功能所需文字与状态对象名。Stitch v2 项目已废弃，不再用于生产实现，`scratch/` 中 21 个截图、提示词和生成稿已清理。
- 仍保留交易与分析功能：Longbridge 只读行情源、事务式数据源切换、每数据源独立品种、Token 状态；多 AI 档案保存/真实测试/原子切换、Thinking 与 effort 适配；自由追问线程生命周期修复。
- Longbridge 仅使用 `QuoteContext`，不会创建交易上下文或下单；凭据从进程环境或 `D:\Desktop\Quant\env` 读取。
- 实盘扩展已进入需求冻结：OKX 第一阶段优先实现黄金现货类产品与黄金永续，架构保留后续增加其他品种的能力，不做永久单品种硬编码。官方公开接口已确认 `XAUT-USDT` 为代币化黄金现货、`XAU-USDT-SWAP` 为跟踪现货黄金的 USDT 永续；正等用户确认首期产品组合。
- OKX API Key/Secret 已按用户授权写入仓库外的 `D:\Desktop\Quant\env`，Passphrase 尚缺，`OKX_LIVE_ENABLED=false`；未连接私有接口、未发单。实盘启用前仍需关闭 API 提币权限并完成账户级只读预检。
- 已新增 `docs/GPT5_6SOL_HANDOFF.md`，将当前实现、Longbridge/OKX 目标、候选架构、安全不变式与未决业务分叉整理为只读移交材料。功能代码与首版移交说明已随 `3d9353f` 发布；PA_Agent 安全源码 ZIP 将由独立的 `gpt-5.6-sol` 高推理任务只读审查。

## 当前验证

- 本轮新增/修改单元测试：232 通过 / 0 失败。
- 相关包 `compileall`：通过。
- 2026-07-16 14:22 使用 Python 3.12.12、pytest 9.1.1 和项目内隔离临时目录重跑 17 个变更相关测试文件：232 通过 / 0 失败，退出码 0，用时 4.156 秒；精确命令与文件清单见 `docs/VALIDATION_EVIDENCE.md`。
- 同日非 live 全量测试由 watchdog 设置 180,000 ms 硬超时；超时前只输出 1 个失败标记且没有形成汇总，清理后无残留进程。因此只能认定“全量未完成且至少出现失败”，不能宣称全量通过或给出最终失败数量。
- 回退边界已由两个只读审计代理交叉核对；独立严格复验已从六个维度确认代码实现与自动化验证完整，无必须修复项。
- 用户按 Esc 停止了 Windows 界面控制；本轮没有继续操作或重启当前 PA_Agent 进程。代码回退需下次启动后显示，当前窗口状态不作为本轮验证依据；不同 DPI 下的可见窗口人工验收留待下次启动。

## 已知边界

- Longbridge 特殊半日市缺少可靠提前收盘时刻，可能延迟到常规收盘才标记闭合；Legacy Token 到期仍需人工更新。
- 连接探测有界面级截止时间和单一在途槽位；若第三方 SDK 永久无视超时，需重启 PA_Agent 才能清掉该阻塞线程。
- 后续工作聚焦行情、分析与交易本身，不继续 Stitch 或视觉重设计。
